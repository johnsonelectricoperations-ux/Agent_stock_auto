# Context Notes

작업 중 내린 결정과 그 이유를 계속 덧붙인다. 새 세션은 이 파일을 먼저 읽는다.

> 2026-09-24 전면 개정. 에이전트 기반 7프로세스 구조로 재설계했다.
> 이전 설계의 결정(D-001~D-024) 전문은 git 이력에 남아 있다.
> 아래 D-025에 승계·폐기 내역을 정리했다.

---

## 2026-09-24 — 설계 전면 개정

### D-025. 에이전트 기반 7프로세스 구조로 재설계

**결정.** 이전 설계(관찰 단계 중심, GitHub Actions 실행)를 폐기하고,
프로세스 분리와 권한 분리를 축으로 하는 구조로 전환한다.

**승계한 결정.** 새 설계의 핵심 원칙과 일치하므로 그대로 가져간다.

| 이전 | 새 설계에서 |
|---|---|
| D-001 주문 경로 LLM 배제 | 핵심원칙 2와 동일 |
| D-009 미검증 파라미터 플레이스홀더 | 핵심원칙 4 (`<USER_DEFINED>`)와 동일 |
| D-011~D-013 비용 게이트 | plan.md 7절. 규칙 탐색 루프의 선행 게이트로 승격 |
| D-016 당일 전량 청산 | Watchdog 강제청산으로 구현 |
| D-023 무인 운용 안전 원칙 | Watchdog + 서버 측 조건주문으로 정식화 |
| D-002 메커니즘 강제·사전등록·전량기록 | 규칙 탐색 루프의 과최적화 방지에 흡수 |
| D-004 look-ahead 차단 | 최종 홀드아웃 **파일 권한 분리**로 강화 |
| D-010 A/B 분리 측정 | Jev 게이트 A/B (P6) |

**폐기한 결정.**

| 이전 | 폐기 사유 |
|---|---|
| D-017 관찰 단계 | 규칙 탐색 루프가 역할을 대체한다. 사람의 관찰 대신 백테스트+검증 게이트로 간다 |
| D-020/D-022 GitHub Actions 실행 | 7개 프로세스 상시 운영이 불가능하다. 상시 실행 환경으로 간다 |
| D-021 관찰 대조군 | 관찰 단계가 없어져 적용 대상이 사라졌다. 다만 "분모를 기록한다"는 정신은 탐색 루프의 전체 시험 횟수 기록으로 승계 |
| D-024 수집기=데이터레이어 | ① Collector로 대체 |

**코드 처리.** 지우지 않고 남겨 둔다. 되돌릴 수 있는 상태를 유지한다.

| 파일 | 상태 |
|---|---|
| `costgate/` | **사용.** plan.md 7절 |
| `appconfig.py` | **사용.** 핵심원칙 4 강제. 플레이스홀더 표식을 `<USER_DEFINED>`로 맞출 것 |
| `collector/storage.py` | 원자적 쓰기 개념만 승계. 이벤트 로그는 SQLite로 간다 |
| `collector/kis_client.py` | 증권사 확정 전까지 보류 |
| `collector/selection.py` | **사용.** D-032로 부활. 분봉 수집 대상 선정에 쓴다 |
| `.github/workflows/` | **폐기 예정.** 상시 실행 환경으로 이전 후 제거 |

---

### D-026. Jev를 선택적 부품으로 둔다

**결정.** ③ Jev Gate 없이도 전체 시스템이 정상 동작해야 한다.
Jev는 마지막에(P6) 붙이고, A/B로 기여도를 분리 측정한다.

**이유.** Jev 얼리 액세스 확보 여부가 미결이다.
**아직 쓸 수 있을지 모르는 것에 매매 경로를 의존시키면 안 된다.**
확보에 실패하면 ③이 빈 채로 전체가 멈춘다.

추가로, 얼리 액세스를 받더라도 두 가지가 더 검증돼야 한다.
- **한국어 공시 분류 성능.** 성능이 낮으면 ③을 붙일 가치가 없다.
- **확률 보정(calibration).** 보정되지 않은 확률은 숫자일 뿐이다.
  "0.8"이 실제로 80%의 빈도로 맞지 않으면 임계값 설정이 무의미하다.

**결과.** 폴백(`skip_entry` / `rules_only`)은 예외 처리가 아니라 **기본 동작 경로**다.
Jev가 영구히 없는 상태를 정상 상태로 간주하고 설계한다.

---

### D-027. 텔레그램 화이트리스트 — 봇 경로의 유일한 방어선

**결정.** 두 봇 모두 **등록된 chat_id 외의 모든 입력을 무시**한다.
특히 Watchdog 봇의 `/flatten`과 Hermes 봇의 규칙 승인이 대상이다.

**이유.** 텔레그램 봇은 토큰을 아는 사람이면 누구나 메시지를 보낼 수 있다.
봇 이름을 검색해 찾을 수도 있다. 방어선이 없으면 두 가지 공격이 가능하다.

1. **`/flatten` 남용** — 임의로 전량 청산시켜 손실을 강제한다
2. **악성 규칙 승인** — 이쪽이 더 위험하다. 승인은 실제 매매 규칙을 바꾼다

**결과.**
- chat_id 화이트리스트를 코드가 아니라 설정에 두고, 미설정 시 기동을 거부한다
- 규칙 승인 시 **규칙 내용의 해시를 함께 표시**해, 승인 대상이 바뀌지 않았음을 확인한다
- 봇 토큰이 유출되면 화이트리스트가 유일한 방어선임을 문서에 명시한다

---

### D-028. 권한은 코드 약속이 아니라 OS로 강제한다

**결정.** 아래 두 가지를 **파일 권한 수준**에서 분리한다.

1. **Hermes의 DB 읽기 전용** — 쓰기 권한이 없는 계정으로 실행한다
2. **최종 홀드아웃 데이터** — Hermes가 읽을 수 없는 권한으로 둔다

**이유.** "Hermes는 읽기만 합니다"는 코드 약속이고, 코드는 바뀐다.
LLM 에이전트는 도구를 조합해 예상 못 한 경로로 접근할 수 있다.
그리고 홀드아웃 누출은 **조용히 일어나고 나중에 알아챌 방법이 없다.**
성과가 좋게 나왔을 때 그게 실력인지 누출인지 구분이 안 된다.

**이유 (SQLite).** 7개 프로세스가 하나의 DB에 접근하면 락 경합이 발생한다.
WAL 모드를 켜서 읽기가 쓰기를 막지 않게 한다.

**결과.**
- 프로세스별 실행 계정 분리
- Hermes 계정은 DB에 읽기 권한만, 홀드아웃 경로에는 권한 없음
- 검증 방법 — Hermes 프로세스에서 쓰기·홀드아웃 읽기를 시도해 **OS가 거부하는지** 확인한다

---

### D-029. 규칙 승인 요청은 장후에만, 무응답은 현상 유지

**결정.** 승인 요청을 장중에 발송하지 않는다. 그리고 **무응답을 승인으로 처리하지 않는다.**

**이유.** 운용자가 직장 근무 중이라 장중에 텔레그램을 볼 수 없다.
장중에 요청이 오면 두 가지 중 하나가 일어난다.
- 못 보고 지나간다 (요청이 무의미해진다)
- 급하게 확인 없이 승인한다 (승인 절차가 형식이 된다)

둘 다 핵심원칙 1을 무력화한다.

**결과.**
- 승인 요청은 장 마감 후 일괄 발송
- 응답이 없으면 기존 설정 버전이 그대로 유지된다. 기본값은 항상 "변경 없음"이다
- 승인 만료 시간을 두고, 만료되면 재승인이 필요하다

---

### D-030. 증권사 선택의 1순위 기준은 서버 측 조건주문 지원 여부

**결정.** 증권사를 고를 때 수수료보다 **서버 측 조건주문(스탑로스) 지원**을 먼저 본다.

**이유.** Watchdog도 프로세스다. 죽을 수 있다.
프로세스 다운, 네트워크 단절, 서버 자체 장애 — 내 쪽이 전부 죽는 상황이 존재한다.
그 순간 포지션은 보호 장치 없이 시장에 노출된다.
운용자는 직장에 있어 인지조차 못 한다.

**내 서버가 전부 죽어도 증권사 서버는 살아 있다.**
주문 시점에 손절을 증권사 쪽에 걸어두면, 내 시스템의 생존과 무관하게 손실이 한정된다.
이것이 유일한 최후 안전망이다.

**결과.**
- 미지원 증권사는 무인 운용 대상에서 제외하거나, 대체 안전망을 설계해야 한다
- 지원 시 Executor가 주문 체결과 **동시에** 조건주문을 등록한다
- 검증 방법 — 주문 후 모든 프로세스를 강제 종료하고, 증권사 측 손절이 유효한지 확인한다

---

### D-031. 자율성 범위 확정 — 규칙은 자율, 한도는 사람 (핵심원칙 1 개정)

**결정.** 에이전트가 규칙을 설계·검증·배포까지 자율적으로 수행한다.
실전 투입에 사람 승인을 요구하지 않는다. 대신 **안전을 하드 리밋으로 확보한다.**

| 영역 | 누가 바꾸나 | 강제 방식 |
|---|---|---|
| 매매 규칙 (무엇을 언제 사고 파나) | **에이전트 자율** | — |
| 하드 리밋 (일 손실 한도, 종목당 노출, 최대 포지션) | **사람만** | 파일 권한 분리 |
| Watchdog 설정 (강제청산 시각, 킬스위치) | **사람만** | 파일 권한 분리 |

**이유.** 운용자가 직장 근무 중이라 승인이 병목이 된다.
D-029에서 짚었듯 장중 승인 요청은 무시되거나 형식적 승인이 되어 절차가 무의미해진다.
안전을 "사람이 매번 확인한다"에 걸면 그 확인이 형식이 되는 순간 안전장치가 사라진다.
**안전을 구조에 걸면 사람이 자리에 없어도 유지된다.**

에이전트가 아무리 이상한 규칙을 만들어도 손실은 한도 안에 갇힌다.
한도 파일은 에이전트 실행 계정에 쓰기 권한이 없다 (D-028과 같은 방식).

**유지되는 것.** 핵심원칙 2·3은 그대로다. 주문은 결정론적 코드만 내고,
Watchdog은 LLM과 무관하게 동작한다. 자율성이 열린 것은 **규칙 계층뿐**이다.

**결과 — 텔레그램의 역할이 바뀐다.**

| 이전 | 이후 |
|---|---|
| 규칙 승인 (진행 조건) | 규칙 배포 **통보** (사후) |
| — | 이의 제기 및 롤백 지시 |
| `/stop` `/flatten` `/status` | 동일 |

**한도 변경은 텔레그램으로도 불가능하게 둔다.** 서버에 직접 접근해야 한다.
한도가 마지막 방어선인데 그 변경 경로를 메신저에 열어두면 방어선이 메신저 보안 수준으로 내려간다.
D-027의 화이트리스트는 그대로 유지하며, 오히려 더 중요해진다.

**D-029 개정.** 승인 요청이 사라졌으므로 "장후에만 발송"은 **통보**에 적용한다.
무응답은 여전히 현상 유지가 아니라 **진행**이다. 되돌리려면 명시적 롤백 지시가 필요하다.

---

### D-032. 수집 범위는 "규칙이 쓰는 것"이 아니라 "규칙이 만들어질 수 있는 것"

**결정.** ① Collector는 현재 규칙이 참조하는 필드가 아니라,
**앞으로 제안될 규칙이 참조할 수 있는 원자료 전반**을 수집한다.

**이유.** 에이전트가 규칙을 자율 설계한다면(D-031), 내일 어떤 조건 블록을 제안할지 오늘 알 수 없다.
제안 시점에 과거 데이터가 없으면 백테스트를 할 수 없고, 백테스트를 못 하면 검증도 못 한다.
**수집하지 않은 데이터는 영원히 쓸 수 없다.** 지나간 분봉은 다시 구할 수 없기 때문이다.

곧 수집 범위가 **탐색 가능 공간의 상한**을 결정한다. 좁게 모으면 에이전트의 자율성도 그만큼 좁아진다.

**원칙 1 — 원자료를 저장하고 파생 지표는 저장하지 않는다.**

지표 정의는 에이전트가 바꾼다. 원자료가 있으면 언제든 다시 계산되지만,
지표만 저장해 두면 정의가 바뀌는 순간 과거 전체가 쓸모없어진다.

**원칙 2 — 분모를 함께 모은다.**

"이 조건에서 올랐다"만으로는 규칙이 검증되지 않는다.
"같은 조건인데 안 오른 경우"가 있어야 한다. 오른 종목만 모으면
에이전트가 반드시 생존 편향에 빠진 규칙을 만들어낸다.

**원칙 3 — 체결 품질을 실측한다.**

주문 의도가와 실제 체결가의 차이를 기록한다.
슬리피지 가정이 틀리면 백테스트 전체가 무의미해진다 (plan.md 8절).

**수집 구성.**

| 대상 | 범위 | 역할 |
|---|---|---|
| 일봉 + 투자자별 수급 | **전 종목** | 분모. 가볍다 |
| 분봉 + 호가 | **선정 종목** | 상세 분석. 무겁다 |
| 공시 | 전 종목 | Jev 입력, 이벤트 규칙 재료 |
| 종목 상태 | 전 종목 | 거래정지·관리종목·투자경고 |
| 체결 기록 | 자기 주문 | 슬리피지 실측 |

전 종목 분봉은 API 호출 제한상 불가능하다.
따라서 선정이 필요하고, 선정은 **상위군 + 무작위 대조군**으로 구성한다.

**결과 — D-021 부활.** 관찰 단계는 폐기됐지만 대조군 개념은 여기서 다시 필요해진다.
`collector/selection.py`를 보류에서 **사용**으로 되돌린다.

---

### D-033. 종목 선정을 3층으로 분리한다 — look-ahead 차단

**결정.** 선정을 성격이 다른 세 층으로 나누고, **C층 데이터를 진입 조건에 직접 쓰지 않는다.**

| 층 | 시점 | 무엇으로 정하나 | 용도 |
|---|---|---|---|
| **A. 사전 유니버스** | 장 시작 전 | 전일까지 확정된 정보 | 매매 대상 모집단 |
| **B. 장중 동적 편입** | 장중 | 그 시점에 계산 가능한 것 | **실제 매매** |
| **C. 사후 수집 선정** | 장 마감 후 | 당일 결과 | **분석 재료** |

**이유.** 사후 선정은 답을 보고 고르는 것이라 그 자체가 미래 정보다.

```
장 마감 후 "오늘 거래대금 1위는 A" → A의 분봉 수집
        ↓
규칙 발견: "거래대금 1위가 10시에 눌리면 매수" → 백테스트 승률 60%
        ↓
실전 09:00 — 오늘 거래대금 1위가 뭐가 될지 모른다
```

규칙이 **미래를 안다는 전제** 위에 서 있어 백테스트만 되고 실전에서는 재현되지 않는다.
에이전트가 규칙을 자율 생성하면(D-031) 이 함정에 반드시 빠진다.
사람이 조심하는 것으로는 막을 수 없다.

**C층의 올바른 용도.** C층 데이터로 찾는 것은 이것이다.

> "이 종목이 움직이기 **시작하기 전에** 장중에 감지할 수 있는 신호가 있었나?"

즉 C층 데이터로 **B층 조건을 발굴**한다. C층 자체는 조건이 되지 않는다.

**강제 방법 — 조건 블록에 `가용시점`을 붙인다.**

```yaml
- 블록: 전일_평균거래대금_대비      # 가용시점: 장전
- 블록: 09시30분까지_누적거래대금    # 가용시점: 09:30 이후
- 블록: 당일_종가등락률             # 가용시점: 장마감 → 진입 조건 사용 불가
```

백테스트 엔진이 **진입 시각보다 늦게 확정되는 블록을 참조하는 규칙을 자동 기각**한다.
D-004(look-ahead 차단)의 실제 구현 방식이다.

---

### D-033-1. 고정 코호트 추가 — 기준선 확보

**결정.** C층 수집 대상에 **고정 코호트**를 추가한다. 최초 1회 무작위로 뽑고 이후 계속 유지한다.

| 구분 | 어떻게 뽑나 | 역할 |
|---|---|---|
| 상위군 | 매일 새로 (그날 움직인 것) | 신호 후보 |
| 대조군 | 매일 무작위 | 분모 |
| **고정 코호트** | **1회 추첨 후 고정** | **기준선** |

**이유.** 대조군은 매일 새로 뽑아 시계열이 끊긴다. 분모는 되지만 기준선은 되지 못한다.
"이 종목의 평소"를 알아야 "오늘이 특이한지" 판단할 수 있고, 그러려면 같은 종목을 계속 봐야 한다.

**빠진 종목을 대체하지 않는다.** 상장폐지·거래정지로 코호트에서 빠져도 기록만 하고 빈자리를 채우지 않는다.
대체하면 살아남은 종목만 남아 또 생존 편향이 생긴다.

**결과.** `collector/selection.py`에 `draw_cohort()`를 추가하고,
`select_candidates()`가 코호트를 필수 인자로 받는다. 빈 코호트는 거부한다.
수집 첫날에 뽑는다. 빨리 뽑을수록 기준선이 길게 쌓인다.

---

## 2026-09-24 (4차) — 외부 도구 실사, 인프라 확정

### D-034. Hermes Agent 확인 — 범용 에이전트를 제한 역할로 가둔다

**확인된 사실** (공식 저장소·문서 기준).

| 항목 | 내용 |
|---|---|
| 라이선스 | MIT 오픈소스 |
| 출시 | 2026년 2월 |
| 설치 | Linux/macOS/WSL2 원라이너. 의존성 자동 설치 |
| 요구 사양 | "$5 VPS에서도 동작" |
| 모델 백엔드 | **제공자 무관.** Nous Portal, OpenRouter, OpenAI, 커스텀 엔드포인트. `hermes model`로 교체 |
| 기능 | 지속 메모리, **스킬 자율 생성·자기 개선**, 내장 크론, 메시징 게이트웨이(텔레그램 포함), 서브에이전트 병렬 생성 |
| 저장 위치 | `~/.hermes` — 파일시스템 쓰기 필요 |

**결정 1 — 현재 문서의 "읽기 전용" 표현을 수정한다.**

Hermes는 자기 영역에 계속 써야 한다. 정확한 경계는 이렇다.

```
~/.hermes/          쓰기 허용    자기 영역 (메모리·스킬·설정)
이벤트 로그 DB       읽기만       OS 권한으로 강제
limits/  holdout/   접근 불가    OS 권한으로 강제
```

> **"Hermes는 읽기 전용"이 아니라 "Hermes는 자기 영역 밖에 쓰지 못한다"가 맞다.**

**결정 2 — 매매 경로 스케줄링에 Hermes 내장 크론을 쓰지 않는다.**

편리해서 쓰고 싶어지는 자리다. 하지만 장전 점검·진입 마감·강제청산이 Hermes 크론에 걸리면
LLM이 멈출 때 매매 안전장치도 함께 멈춘다. 핵심원칙 3이 무너진다.

```
매매 경로 (장전 점검, 진입 마감, 강제청산)  →  systemd
Hermes 자기 작업 (리포트, 규칙 탐색)        →  Hermes 크론 허용
```

**결정 3 — 텔레그램 통합은 프롬프트 인젝션 경로다.**

Hermes가 텔레그램으로 대화하면 수신 메시지가 에이전트 컨텍스트에 들어간다.
누구든 봇을 찾아 지시를 주입할 수 있다. D-027의 chat_id 화이트리스트가 **권장이 아니라 필수**다.
핵심원칙 2(주문 권한 없음) 덕분에 최악이 "이상한 리포트"에서 멈춘다.

**결정 4 — 서브에이전트 병렬 생성에 비용 상한을 건다.**

규칙 탐색에 유용하지만 API 호출이 예상 밖으로 늘 수 있다. 월 상한을 설정에 둔다.

**파생 효과.** Hermes 봇을 직접 구현할 필요가 없다. 내장 게이트웨이를 쓴다.
단 **Watchdog 봇은 반드시 별도로 직접 만든다.** Hermes를 거치면 LLM 중단 시 `/flatten`이 죽는다.

---

### D-035. Jev 확인 — 보정 검증을 진입 게이트 사용의 필수 관문으로

**확인된 사실.**

| 항목 | 내용 |
|---|---|
| 출시 | **2026년 9월 15일.** 이 결정 시점 기준 9일 |
| 접근 | 얼리 액세스 대기열. Vercel AI Gateway `typesafe-ai/jev`로 우회 가능 |
| 성격 | 텍스트 생성 없음. 타입 있는 값 + 신뢰도 반환 |
| 질문 유형 | one-of-N(최대 255), 2~10단계 척도, 예/아니오 확률. 병렬 평가 |
| 속도 | LLM 대비 2자릿수 빠름 |
| 비용 | 입력 100만 토큰당 $0.042, 출력 무료 |

**잘 맞는 점.** 속도 때문에 D-001이 지적한 "LLM 지연 1~10초" 제약이 이 부품에는 적용되지 않는다.
타입 출력이라 파싱이 불필요해 명세의 "JSON 스키마 강제"보다 강하다.
자연어 지시를 받지 않아 인젝션 표면도 작다.

**결정적 위험 — 우리 도메인에서 보정이 검증되지 않았다.**

TypeSafe 문서가 스스로 밝힌다. 신뢰도 임계값은 용도별로 다르므로 **고객 자신의 데이터로 테스트해야** 하며,
**공표된 보정 보장이 없다.** 그리고 분포 이동·적대적 입력·롱테일 레이블 하에서
보정이 유지되는지는 **열린 질문**으로 남아 있다.

한국 소형주 수급 패턴은 명백히 학습 분포 밖이다. 보정이 깨지기 딱 좋은 자리다.

```
임계값 0.75로 설정  →  실제 적중률 55%
        ↓
게이트가 게이트 역할을 못 하는데 있다고 믿게 된다
        ↓
게이트가 아예 없는 것보다 나쁘다
```

**결정 — 보정 곡선 측정을 통과하기 전에는 진입 게이트로 쓰지 않는다.**

```
1. 섀도 기간에 주문 없이 예측만 기록
2. 보정 곡선 측정 — 0.7을 준 케이스들의 실제 발생률이 70%인가
3. 구간별 표본이 충분히 쌓일 때까지 대기
4. 보정이 확인된 구간에서만 임계값 설정
5. A/B로 기여도 분리 (P6)
```

한국어 공시 분류 성능은 **보정과 별개의 측정 대상**이다.

**D-026 유지.** 선택적 부품으로 둔다는 결정은 그대로다. 근거만 갱신된다.
접근 불가 우려는 해소됐지만, 도메인 보정 미검증과 **출시 9일이라는 성숙도**가 더 큰 이유가 됐다.
API·가격·정책이 바뀔 수 있는 단계의 제품에 매매 경로를 묶지 않는다.

**부수 사항.** Jev는 점수만 주고 근거를 주지 않는다. 다만 "0.73이었고 임계값 0.65를 넘어 진입"은
기록되고 재구성된다. LLM보다는 감사 가능성이 낫다.

---

### D-036. AWS 인프라 확정

**결정.** 상시 실행 환경을 AWS로 하고 아래를 고정한다.

| 항목 | 결정 | 근거 |
|---|---|---|
| 리전 | **서울 (ap-northeast-2)** | 해외 IP 제한 회피, 주문 지연이 곧 슬리피지 |
| 인스턴스 | **작은 CPU 인스턴스** | Hermes가 API 방식으로 동작해 GPU 불요 (D-034) |
| 약정 | 하지 않음 | 검증 전 1년 묶으면 접어도 비용이 나간다 |
| 접속 | **SSM Session Manager** | 인바운드 포트 0개. SSH 개방 안 함 |
| 아웃바운드 | 제한 | 증권사·텔레그램·모델 API·패키지 저장소만 |

**명세 10절의 "Hermes 두뇌로 붙일 모델 선택"이 여기서 해소된다.**
Hermes가 제공자 무관이므로 로컬 GPU를 띄울 이유가 없다. API 방식으로 간다.

**계정 분리 — D-028/D-031을 OS 수준에서 구현하는 자리.**

| 계정 | 가진 것 | 못 가진 것 |
|---|---|---|
| `executor` | 증권사 API 키 | — |
| `hermes` | DB 읽기, `~/.hermes` 쓰기 | DB 쓰기, `limits/`, `holdout/`, API 키 |
| `watchdog` | 청산 권한 | 신규 진입 |

**백업 — 원자료는 다시 못 구한다.**
디스크가 날아가면 몇 달치 탐색 공간이 사라진다 (D-032). 일별 S3 동기화 또는 스냅샷 자동화.

**서버 밖 감시자 — 서버가 죽으면 Watchdog도 죽는다.**
인스턴스 상태 알람을 AWS 바깥 경로로 받는다. D-030의 증권사 조건주문이 손실을 막고,
이 알람이 "죽었다는 사실"을 알린다. 둘은 다른 문제다.

**AWS 계정 자체가 서버보다 큰 위험일 수 있다.**
루트 MFA, 액세스 키를 코드·깃에 넣지 않기, 최소 권한, **비용 알람**.
키가 유출되면 마이닝 인스턴스가 떠서 청구서가 수백만원 나온다.

**미검증.** AWS 서울은 한국 IP지만 **데이터센터 대역**이다.
증권사가 개인 회선과 다르게 취급하는지 확인되지 않았다.
**본격 구축 전에 작은 인스턴스를 한 시간 띄워 접속 확인부터 한다.** 이게 안 되면 나머지가 무의미하다.

---

## 승계된 핵심 수치 (비용 게이트)

2026년 1월부터 매도 시 총 0.20%가 부과된다.
코스피는 증권거래세 0.05% + 농어촌특별세 0.15%, 코스닥은 증권거래세 0.20%다.
**손실 매도에도 부과되므로 승률과 무관한 고정비다.**

왕복 마찰 `f = 매수수수료 + 매도수수료 + 0.20% + 슬리피지`

| 시나리오 | f |
|---|---|
| 낙관 (대형주·지정가) | 약 0.26% |
| 표준 (중형주) | 약 0.33% |
| 보수 (소형주·시장가) | 약 0.43% |

본전 승률 `p = (f/L + 1) / (R + 1)`

손절폭을 좁힐수록 고정비의 상대 비중이 커져 요구 승률이 급등한다.
촘촘히 끊는 쪽이 안전해 보이지만 구조적으로 전략을 죽인다. 직관과 반대이므로 명시해 둔다.

구현은 `costgate/`에 있고, 위 표를 테스트로 고정해 두었다.

---

## 코딩 규약 (프로젝트 적용분)

- 새 소스 파일 첫 줄에 역할을 설명하는 한 줄 한국어 주석을 단다.
- 커밋은 한 문장으로 설명 가능한 논리 단위로 끊는다.
- 코드를 건드렸으면 완료 보고 전에 테스트를 돌린다.
- 인접 코드를 "개선"하지 않는다. 변경된 모든 줄은 요청에 직접 추적되어야 한다.
- 비밀값(API 키, 봇 토큰)은 코드·커밋·로그에 절대 넣지 않는다.
