# Context Notes

작업 중 내린 결정과 그 이유를 계속 덧붙인다. 새 세션은 이 파일을 먼저 읽는다.

---

## 2026-09-18 — 초기 설계 결정

### D-001. 에이전트를 실행 계층에서 배제

**결정.** 주문 실행, 포지션 사이징, 손절, 최대 노출 한도는 순수 코드. LLM 호출 없음.

**이유.**
- 동일 입력에 동일 출력이 보장되지 않는다. temperature=0으로도 완전히 같지 않다.
- 지연시간이 초 단위다.
- 사고 시 "왜 그 주문을 냈는가"를 재구성할 수 없다.

**결과.** 에이전트 계층과 룰 엔진 사이에 JSON 스키마 경계를 둔다.
에이전트는 후보와 근거만 넘기고, 임계값이나 한도는 건드릴 수 없다.

---

### D-002. 기준 위임 수준 — (A) 발굴까지 허용하되 제약 4종 부착

**결정.** 에이전트가 선정 기준 자체를 발굴하는 것을 허용한다. 단 아래 제약을 강제한다.

1. **메커니즘 서술 강제.** 필터만 제출하면 기각. "저PBR + 자사주 매입"은 필터이고,
   "지배주주에게 주가 방어 유인이 생기는 구간"은 메커니즘이다.
   메커니즘은 반증 가능하지만 필터는 과최적화만 가능하다.
2. **사전 등록과 동결.** 제안 시점에 기준을 동결하고 이후 구간에서만 검증한다.
   제안 → 백테스트 → 수정 → 재백테스트 루프를 돌리는 순간 무효다.
3. **탐색 예산 제한과 전량 기록.** 채택된 가설뿐 아니라 **기각된 가설도 전부 기록**한다.
   분모를 모르면 다중검정 보정이 불가능하다.
4. **무효화 조건 선언.** "이 기준은 어떤 상황에서 작동을 멈추는가"를 제안 시점에 쓰게 한다.
   못 쓰면 채택하지 않는다.

**이유.** 사람은 하루에 전략 3개를 시험하지만 에이전트는 500개를 돌린다.
500개 중 백테스트 성적 좋은 게 나오는 건 당연하고, 그건 발견이 아니라 우연이다.
제약 없이 열면 자동화된 과최적화 기계가 된다.

---

### D-003. 트랙 분리 (시계 분리)

**결정.** 트랙 1(파이프라인, 수 주)과 트랙 2(가설 발굴, 분기)를 분리하고 별도로 진행한다.

**이유.** D-002의 사전등록·전진검증 구조를 쓰면 가설 하나 판정에 최소 분기 단위가 걸린다.
룰엔진+페이퍼는 수 주면 돈다. 한 마일스톤에 묶으면 트랙 2가 병목이 되거나,
일정 압박으로 검증 절차를 건너뛰게 된다.

**결과.** 트랙 1은 고정된 초기 기준으로 먼저 관통시킨다.
트랙 2 게이트를 통과한 가설만 나중에 편입한다.

---

### D-004. 백테스트 look-ahead 오염 차단

**결정.** 에이전트에게 과거 구간을 판단시키는 방식의 백테스트는 유효한 검증으로 인정하지 않는다.

**이유.** LLM은 2023년 데이터를 주고 판단을 물으면 2023년에 무슨 일이 있었는지 이미 알고 있다.
에이전트 기반 백테스트는 거의 항상 비현실적으로 좋게 나오고,
원인은 전략이 아니라 학습 데이터 누출이다.
"발굴"이라고 내놓는 기준이 사실은 기억 재생인 경우도 많다.

**결과.**
- 에이전트 계층 검증은 **전진검증(forward test)만** 인정한다.
- 결정론적 신호 레이어는 일반 백테스트로 검증해도 된다. 여긴 LLM이 개입하지 않는다.
- 이 구분 때문에라도 A/B 하네스(에이전트 유/무 분리 측정)가 필수다.

---

### D-005. 한국 시장을 먼저, 미국은 이식하지 않음

**결정.** 한국 시장부터 구축한다. 미국용은 나중에 하더라도 **이식이 아니라 재설계**다.

**이유.** 두 시장에서 팩터 거동이 다르다. 미국은 자사주·M&A로 밸류 갭이 닫히지만
한국은 지배구조 할인과 물적분할 리스크로 잘 안 닫힌다.
같은 저평가 기준이 한쪽에선 진입 신호이고 다른 쪽에선 가치 함정이다.

**결과.** 에이전트 인스턴스, 프롬프트, 검증 로그를 시장별로 분리한다.
공통 기준 풀을 만들지 않는다.

---

### D-006. 한국 시장에서 에이전트의 역할 재정의

**결정.** 에이전트의 주 역할을 "공시 문서 독해"가 아니라
"수급·구조 신호에 대한 맥락 부여 및 메커니즘 가설 생성"으로 둔다.

**이유.** 한국 소형주는 DART 공시 분량과 구조화 수준이 모두 얇고,
애널리스트 커버리지가 사실상 없다. 읽을 텍스트가 없다.
한국 시장의 주 가격 동인은 수급과 테마 전파이고, 이건 텍스트가 아니라 수치·구조 신호다.

**참고.** 기존에 설계했던 외국인 프로그램 매매 패턴 기반 접근의 연장선이다.
그 설계 문서가 있으면 신호 레이어 출발점으로 재사용한다.

---

### D-007. 검증 게이트를 한국용으로 두껍게

**결정.** 에이전트 출력은 DART/KRX 원본 대조를 통과하지 못하면 후보에서 탈락시킨다(하드 게이트).

**이유.** LLM 학습 데이터에서 한국 소형주 밀도가 낮다.
종목명, 사업 내용, 지분 구조를 헷갈리는 빈도가 미국 대비 확연히 높다.

**검증 방법.** 존재하지 않는 종목코드와 틀린 재무수치를 의도적으로 주입해서
게이트가 100% 걸러내는지 테스트한다. 통과율 100% 미만이면 파이프라인을 진행하지 않는다.

---

### D-008. 페이퍼 체결 모델에 한국 고유 제약 반영

**결정.** 아래를 체결 시뮬레이터에 반드시 구현한다.

- 가격제한 ±30%. 상·하한가 도달 시 체결 불가로 처리한다.
- 거래정지, 관리종목 지정, 투자경고/위험 상태 변화
- 소형주 호가 단위와 슬리피지 모델
- 수정주가 처리 — 액면분할, 무상증자, 배당락

**이유.** 이걸 빼면 페이퍼 성과가 구조적으로 부풀려진다.
특히 상한가 종목을 체결된 것으로 처리하면 결과가 완전히 무의미해진다.

---

### D-009. 파라미터 정책

**결정.** 검증되지 않은 수치는 임의 기본값을 넣지 않고 플레이스홀더로 둔다.
설정 로딩 시 플레이스홀더가 남아 있으면 **런타임 에러로 중단**시킨다.

**이유.** 기본값이 들어가면 "누가 정한 건지 모르는 숫자"가 조용히 운용에 들어간다.
명시적으로 막는 편이 낫다.

**현재 미정 파라미터 (전부 사용자 정의 필요).**

| 키 | 설명 |
|---|---|
| `universe.min_market_cap` | 최소 시가총액 |
| `universe.min_avg_turnover` | 최소 일평균 거래대금 |
| `universe.min_listing_days` | 최소 상장 경과일 |
| `signal.flow_persistence_window` | 수급 지속성 판정 기간 |
| `signal.entry_threshold` | 진입 임계값 |
| `signal.exit_threshold` | 청산 임계값 |
| `risk.max_position_weight` | 종목당 최대 비중 |
| `risk.max_concurrent_positions` | 최대 동시 보유 종목 수 |
| `risk.max_drawdown_halt` | 운용 중단 낙폭 한도 |
| `agent.max_candidates` | 에이전트 후보 제출 상한 |
| `hypothesis.max_proposals_per_quarter` | 분기당 가설 제안 상한 |
| `hypothesis.min_forward_test_days` | 전진검증 최소 기간 |
| `hypothesis.significance_level` | 다중검정 보정 후 채택 유의수준 |

**미확정 기술 결정 (착수 전 확정 필요).**
- 시세·수급 데이터 소스 (KIS API / KRX 직접 / 유료 벤더 중 택일)
- 페이퍼 실행기를 자체 구현할지, 증권사 모의투자 계좌를 쓸지

---

### D-010. 성과 평가는 반드시 A/B

**결정.** 모든 성과 리포트는 다음 두 계열을 나란히 산출한다.

- 대조군 — 에이전트 없는 룰 엔진 단독
- 실험군 — 에이전트 후보선정 포함

**이유.** 에이전트를 붙였더니 수익이 났다는 건 에이전트 덕분이라는 증거가 아니다.
동일 룰 엔진 단독이 대조군이 되어야 기여도를 분리할 수 있다.

---

## 코딩 규약 (프로젝트 적용분)

- 새 소스 파일 첫 줄에 역할을 설명하는 한 줄 한국어 주석을 단다.
- 커밋은 한 문장으로 설명 가능한 논리 단위로 끊는다.
- 코드를 건드렸으면 완료 보고 전에 테스트를 돌린다.
- 인접 코드를 "개선"하지 않는다. 변경된 모든 줄은 요청에 직접 추적되어야 한다.

## 법적·규정 확인 사항 (미해결)

실거래 전환은 M3 이후 별도 판단이며, 그 전에 아래를 확인해야 한다.
이 문서 작성자는 투자 자문 자격이 없고, 내용은 시스템 설계 관점의 정리다.

- 사용 증권사 API 약관상 자동매매 허용 범위
- 국내 알고리즘 매매 관련 규정

---

## 2026-09-18 (2차) — 당일청산 데이트레이딩으로 전환

### D-011. 매매 프로파일 확정 — 당일청산, 수십 분 보유, 일 1~2회

**결정.** 보유시간 수십 분~장마감, 당일 전량 청산. 일 왕복 1~2회를 상한으로 둔다.
초~수십초 스캘핑은 배제한다.

**이유 (비용 계산).** 2026년 1월부터 증권거래세가 인상되어
코스피는 증권거래세 0.05% + 농어촌특별세 0.15%, 코스닥은 증권거래세 0.20%로
양 시장 모두 **매도 시 총 0.20%**다. 손실 매도에도 부과된다.

왕복 마찰 `f = 매수수수료 + 매도수수료 + 0.20% + 슬리피지`

| 시나리오 | f |
|---|---|
| 낙관 (대형주·지정가) | 약 0.26% |
| 표준 (중형주) | 약 0.33% |
| 보수 (소형주·시장가) | 약 0.43% |

f = 0.33%, 20거래일, 매회 전량 투입 기준 월 마찰 소모.
1회/일 = 6.6%, 2회/일 = 13.2%, 3회/일 = 19.8%, 5회/일 = 33.0%.

3회 이상은 월 20% 이상을 벌어야 본전이므로 상한을 2회로 둔다.

---

### D-012. 손절폭 하한 1%

**결정.** `risk.stop_loss_pct`의 하한을 1%로 둔다. 0.5% 이하는 허용하지 않는다.

**이유.** 본전 승률 `p = (f/L + 1) / (R + 1)`. f = 0.33% 기준 요구 승률.

| 손절폭 L | 1.5:1 | 2:1 | 3:1 |
|---|---|---|---|
| 0.5% | 66.4% | 55.3% | 41.5% |
| 1.0% | 53.2% | 44.3% | 33.3% |
| 2.0% | 46.6% | 38.8% | 29.1% |

거래세 0.20%는 고정비다. 손절폭이 좁을수록 마찰이 상대적으로 커져 요구 승률이 급등한다.
촘촘히 끊는 쪽이 안전해 보이지만 구조적으로 전략을 죽인다. 직관과 반대이므로 명시해 둔다.

**목표 조합.** 일 1~2회, 손절 1~2%, 손익비 2:1 이상, 승률 45% 이상.

---

### D-013. 비용 게이트를 M0으로 전진 배치

**결정.** 전략 코드보다 비용 모델·손익분기 계산기를 먼저 만든다.
모든 전략 아이디어는 코딩 전에 이 게이트를 통과해야 한다.

**이유.** 비용 게이트가 없으면 몇 달을 쓴 뒤에야 구조적 불가능을 발견하게 된다.
아이디어 단계에서 "회당 평균 몇 % 먹어야 하는가"를 역산해 비현실적이면 즉시 버린다.

---

### D-014. D-006 개정 — 에이전트가 장중에 복귀

**결정.** D-006에서 정한 "장전/장후 오프라인 전용" 제약을 완화한다.
보유시간이 수십 분이므로 장중 조건부 재평가에 에이전트를 쓸 수 있다.

| 시점 | 역할 |
|---|---|
| 장전 | 테마·뉴스·공시 기반 감시 종목 압축 |
| 장중 | 후보 조건부 재평가, 수급 이상 구간 맥락 부여 |
| 장후 | 매매 복기, 가정이 깨진 지점 식별 |
| 주기 | 파라미터 드리프트 감지, 가설 생성 |

**불변.** 손절·청산·사이징 경로에는 어떤 시점에도 LLM 호출이 들어가지 않는다 (D-001 유지).

**이유.** LLM 왕복 지연은 1~10초다. 스캘핑에서는 치명적이지만 수십 분 보유에서는 무시 가능하다.

---

### D-015. 기준봉 1분봉 — 틱·호가 데이터 불요

**결정.** 1분봉을 기준 데이터로 쓴다. 틱·호가 데이터는 조달하지 않는다.

**이유.** 스캘핑이었다면 체결 가정이 성과를 지배해 틱 데이터가 필수였다.
수십 분 보유에서는 1분봉 해상도로 충분하다.
개인이 틱·호가 이력을 확보하기 어렵고 비싼 문제를 우회할 수 있다.

**파생 효과.** 실시간 웹소켓 동시구독 한도 압박이 줄어든다.
전 종목 스트리밍 대신 감시 리스트 폴링으로 갈 수 있다.

---

### D-016. 당일청산 로직 추가

**결정.** 아래를 룰 엔진과 체결 모델에 반영한다.

- 장마감 강제청산. 미청산 포지션이 남지 않도록 보장
- 장 마감 동시호가 처리. 강제청산이 동시호가에 걸리는 경우의 체결 가정
- 일중 시간대별 거동 차이 — 시가 변동성, 점심 소강, 마감 전 수급

**검증.** 임의 구간 시뮬레이션 종료 시 오버나이트 포지션 수가 항상 0이어야 한다.

---

### 초기안에서 유지되는 결정

D-001 (실행 계층 LLM 배제), D-002 (기준 발굴 제약 4종), D-003 (트랙 분리),
D-004 (look-ahead 차단, 에이전트 계층은 전진검증만), D-005 (한미 시장 분리),
D-007 (검증 게이트), D-009 (파라미터 플레이스홀더 정책), D-010 (A/B 분리 측정).

D-008은 D-016으로 확장된다.

---

### 추가 미정 파라미터

| 키 | 설명 |
|---|---|
| `cost.commission_buy` | 매수 수수료율 (증권사 확인 필요) |
| `cost.commission_sell` | 매도 수수료율 |
| `cost.slippage_model` | 슬리피지 산정 방식 |
| `risk.stop_loss_pct` | 손절폭 (하한 1%, D-012) |
| `risk.target_rr` | 목표 손익비 (2:1 이상) |
| `trade.max_round_trips_per_day` | 일 왕복 상한 (1~2, D-011) |
| `trade.force_close_time` | 강제청산 시각 |
| `universe.watchlist_size` | 장중 감시 종목 수 |

---

## 2026-09-18 (3차) — 관찰 단계 신설, 데이터 조달 경로 확정

### D-017. 전략 수립 전에 관찰 단계를 둔다

**결정.** 기준·전략을 지금 작성하지 않는다. M0(비용 게이트) 완성 후,
**자동 수집된 관찰 기록을 수 주간 쌓은 뒤에** 메커니즘 가설 작성으로 넘어간다.

**이유.** D-002가 "필터는 기각, 메커니즘만 인정"으로 못박아 놨는데,
메커니즘은 "내가 살 때 반대편에서 파는 사람이 누구이고 왜 파는가"에 대한 답이다.
운용자가 일단타 경험이 없어 지금은 그 답을 쓸 **재료(관찰 데이터)가 없다**.
재료 없이 지금 기준을 쓰면 인터넷에서 가져온 필터가 되고, 이는 D-002가 기각하라고 한 형태다.

**주의.** 이 제약은 에이전트에게만이 아니라 **사람이 쓴 기준에도 동일하게 적용**한다.

---

### D-018. 성과 리포트에 "아무것도 안 하기" 대조군 추가 — D-010 확장

**결정.** D-010의 A/B(에이전트 유/무)에 세 번째 계열을 추가한다.

- 대조군 0 — **코스피200 ETF 매수 후 보유 (무매매)**
- 대조군 1 — 에이전트 없는 룰 엔진
- 실험군 — 에이전트 포함 룰 엔진

**이유.** 시스템이 "그냥 인덱스 들고 있기"를 못 이기면 잘 만든 게 아니라 안 만드는 게 나았던 것이다.
마찰이 0에 가깝고 시간도 들지 않는 선택지가 진짜 기준선이다.
넣는 비용이 거의 없고 판단을 훨씬 정직하게 만든다.

---

### D-019. 데이터 소스 — 한국투자증권(KIS) Open API

**결정.** 시세·수급 데이터 소스를 KIS Open API로 확정한다. (P0 미확정 항목 해소)

**이유.** 운용자가 한투 계좌를 이미 보유하고 있어 API 신청이 가능하다.
1분봉을 제공하므로 D-015가 정한 기준봉에 직접 대응된다.

**한계 (확인 필요).** 무료 API는 과거 1분봉 이력 조회 깊이가 얕다.
다만 D-017의 관찰 단계와 D-004의 전진검증 원칙이 모두 **"오늘부터 앞으로 쌓는"** 구조이므로
과거 이력 부족이 치명적이지 않다. 오히려 정합적이다.

---

### D-020. 실행 위치 — GitHub Actions 크론

**결정.** 장 마감 후 관찰 데이터 수집을 GitHub Actions 스케줄로 자동 실행한다.
운용자가 직장을 다녀 장중·장마감 시각에 개입할 수 없다는 제약에서 나온 결정이다.

**장점.** 무료, PC 상시 가동 불필요, 결과가 저장소에 그대로 누적된다.

**미검증 위험 (선결 과제).** Actions 러너는 해외 IP다.
**KIS API가 해외 IP를 제한하는지 확인되지 않았다.**
따라서 본 수집기보다 **접속 확인용 워크플로를 먼저 돌려 검증**한다.
실패 시 대안은 집 PC 스케줄러 또는 한국 리전 VPS다.

**보안.** API 키는 코드·커밋·로그에 절대 넣지 않는다. GitHub Secrets로만 주입한다.
저장소가 공개 상태인지 먼저 확인하고, 공개면 수집 데이터에 계좌 정보가 섞이지 않도록 한다.
