# Checklist

> 2026-09-24 전면 개정. 에이전트 기반 7프로세스 구조 기준.
> plan.md 11절의 권장 착수 순서를 따른다.

각 단계는 **검증 통과 전까지 다음으로 넘어가지 않는다.**
결정이 생기면 context-notes.md에 D-번호로 덧붙인다.

---

## P0. 계획 문서 (착수 순서 1)

- [x] plan.md 전면 개정
- [x] checklist.md 전면 개정
- [x] context-notes.md 전면 개정
- [x] 비용 게이트 구현 (`costgate/`) — 이전 설계에서 승계
- [x] 설정 로더 구현 (`appconfig.py`) — 핵심원칙 4 강제
- [x] **공용 체결 엔진 구현** (`execution/`) — D-037
- [x] 수집 대상 선정 구현 (`collector/selection.py`) — D-032, D-033

---

## P0-1. 성과 측정 기준 (D-038)

성과 리포트는 **세 계열을 나란히** 산출한다. 하나라도 빠지면 판단이 흐려진다.

- [ ] 대조군 0 — 코스피200 ETF 매수 후 보유 (무매매)
- [ ] 대조군 1 — 룰 엔진 단독 (에이전트 없음)
- [ ] 실험군 — 에이전트 포함
- [ ] 동일 기간·동일 유니버스 보장
- [ ] 마찰 차감 전/후 모두 표시

**검증.** "이 시스템을 만든 게 인덱스를 그냥 들고 있는 것보다 나은가"에 리포트만 보고 답할 수 있다.

---

## P1. 착수 전 확정 사항

### 증권사 (최우선)

- [ ] **서버 측 조건주문(스탑로스) 지원 여부 확인** — 미지원이면 무인 운용 설계 재검토 (D-030)
- [ ] 웹소켓 지원 여부 및 동시 구독 한도
- [ ] API 호출 제한 (초당/분당)
- [ ] 실제 수수료율 확인 (`cost.commission_*`)
- [ ] 약관상 자동매매 허용 범위
- [ ] 모의투자 계좌 제공 여부 (페이퍼 검증에 활용 가능한지)

### 데이터

- [ ] 백테스트용 과거 분봉 조달 경로와 확보 가능 기간
- [ ] 분봉 단위 결정 (1분 / 3분 / 5분)
- [ ] 프로그램 매매 수급 포함 가능 여부
- [ ] 공시 데이터 조달 경로 (DART)

### 실행 환경 — AWS (D-036)

**순서가 중요하다. 1번이 안 되면 나머지가 무의미하다.**

- [ ] **작은 인스턴스 한 시간 띄워 증권사 API 접속 확인** — AWS 서울은 데이터센터 대역이라 미검증
- [x] 리전 확정 — 서울 (ap-northeast-2)
- [x] 인스턴스 급 확정 — 작은 CPU 인스턴스 (Hermes가 API 방식이라 GPU 불요)
- [ ] 약정 없이 온디맨드로 시작
- [ ] SSM Session Manager 설정 — **인바운드 포트 0개**
- [ ] 아웃바운드 제한 (증권사·텔레그램·모델 API·패키지 저장소만)
- [ ] 계정 분리 — `executor` / `hermes` / `watchdog`
- [ ] `limits/` `holdout/` 권한 설정 및 **실제 접근 시험으로 검증**
- [ ] systemd 서비스 등록 (매매 경로 스케줄링. Hermes 크론 아님 — D-034)
- [ ] 재시작 전 잔고 대조 강제
- [ ] 시각 동기화 확인 (NTP) 및 타임존 정책 결정
- [ ] 원자료 백업 — S3 동기화 또는 스냅샷 자동화
- [ ] **서버 밖 감시자** — 인스턴스 상태 알람을 AWS 바깥 경로로
- [ ] 텔레그램 봇 2개 생성, 토큰 발급
- [ ] 비밀값 보관 — Secrets Manager 또는 Parameter Store (`.env` 커밋 금지)

### AWS 계정 보안 (서버보다 큰 위험일 수 있다)

- [ ] 루트 계정 MFA
- [ ] 액세스 키를 코드·깃에 넣지 않음
- [ ] IAM 최소 권한
- [ ] **비용 알람 설정** — 키 유출 시 청구서가 수백만원 나온다

### 도구 실사 (완료)

- [x] Hermes Agent 가용성 확인 — MIT, 2026-02 출시, 원라이너 설치 (D-034)
- [x] Hermes 두뇌 모델 — 제공자 무관. API 방식으로 확정 (D-034)
- [x] Jev 접근 경로 확인 — Vercel AI Gateway로 대기열 우회 가능 (D-035)
- [ ] Hermes 유지보수 상태 지속 관찰 (외부 의존이므로)

**검증.** `pytest` 정상 종료. 임의 종목 1개의 분봉을 받아온다.

---

## P2. ① Collector + ⑤ Executor(페이퍼) — 착수 순서 2

**목표는 "아무것도 매매하지 않는 시스템"을 먼저 완주시키는 것이다.**
신호도 규칙도 없이, 수집과 기록만 하루 종일 무인으로 돈다.

### 이벤트 로그 DB

- [ ] SQLite 스키마 설계 (주문, 체결, 시세 스냅샷, 프로세스 이벤트)
- [ ] WAL 모드 활성화 (다중 프로세스 동시 접근 — D-028)
- [ ] 모든 이벤트에 단조 증가 시각과 발생 프로세스 기록
- [ ] 주문별 고유 ID 체계

### ① Collector — 에이전트의 탐색 공간을 결정한다 (D-032)

수집하지 않은 데이터는 영원히 쓸 수 없다. 넓게 모으고 원자료로 저장한다.

**전 종목 (분모 역할, 가볍다)**
- [ ] 일봉 수집 — 시가/고가/저가/종가/거래량/거래대금
- [ ] 투자자별 수급 수집 (외국인/기관/개인/프로그램)
- [ ] 종목 상태 수집 (거래정지, 관리종목, 투자경고)
- [ ] 공시 수집 (DART)

**선정 종목 (상세 분석, 무겁다) — C층 (D-033)**
- [ ] **고정 코호트 최초 추첨** (`draw_cohort`) — 수집 첫날에. 빨리 뽑을수록 기준선이 길다
- [ ] 코호트를 파일에 저장하고 매일 같은 것을 사용
- [ ] 수집 대상 선정 — **상위군 + 대조군 + 고정 코호트** (`select_candidates`)
- [ ] 코호트에서 빠진 종목 기록 (대체하지 않음)
- [ ] 선정 근거 기록 — 어떤 기준으로 누구를 골랐는지
- [ ] 분봉 수집
- [ ] 호가 수집

**A층 — 사전 유니버스 (전일 정보로만, D-033)**
- [ ] 전일까지 확정된 정보로 유니버스 산출 (시총, 평균 거래대금, 상장 경과일)
- [ ] 제외 종목 조건 적용 (관리종목, 투자경고, 거래정지)
- [ ] 당일 정보가 섞이지 않음을 검증

**B층 — 장중 동적 편입 (D-033)**
- [ ] 편입 조건이 그 시점에 계산 가능한지 검사
- [ ] 편입 시각과 근거 기록

**공통**
- [ ] 파생 지표를 저장하지 않고 원자료만 저장 (정의가 바뀌어도 재계산 가능하게)
- [ ] 수집 실패 시 재시도 및 실패 기록 (조용한 누락 금지)
- [ ] 시세 끊김 감지
- [ ] API 호출 제한 실측 후 선정 규모 확정

### ⑤ Executor (페이퍼 모드)

체결 판정은 **공용 체결 엔진(`execution/`)을 쓴다.** 여기서 따로 만들지 않는다 (D-037).

- [ ] 주문 인터페이스 정의 (페이퍼/실전 동일 인터페이스)
- [ ] 공용 체결 엔진 연결
- [ ] 호가 단위 표를 설정에서 읽어 주입
- [ ] API 키를 이 프로세스만 보유하도록 분리

### 잔고 대조

- [ ] 증권사 잔고 조회
- [ ] 내부 기록과 대조
- [ ] 불일치 시 다음 날 매매 잠금 플래그 기록

### 체결 품질 실측 (D-032 원칙 3)

- [ ] 주문 의도가 기록
- [ ] 실제 체결가 기록
- [ ] 슬리피지 실측값 집계
- [ ] 백테스트 슬리피지 가정과 실측값 대조

**검증.** 실측 슬리피지가 비용 게이트에 넣은 가정과 오차 범위 내 일치한다.
어긋나면 가정을 실측값으로 갱신하고 기존 백테스트 결과를 재평가한다.

**검증.**
- 하루치가 사람 개입 없이 완주한다
- 동일 데이터로 재실행 시 결과가 완전히 일치한다
- 실현 마찰 합계가 비용 게이트 예측치와 오차 범위 내 일치한다
- 상한가 종목 매수 주문이 미체결 처리된다
- 거래정지 종목 주문이 거부된다
- **실전 주문 코드가 리포지토리에 존재하지 않음을 코드 검색으로 확인**

---

## P3. ② Strategy + ④ Risk Mgr — 착수 순서 3

### 규칙 표현

- [ ] 조건 블록 정의 (사전 정의된 집합)
- [ ] **각 조건 블록에 `가용시점` 명시** (장전 / 특정 시각 이후 / 장마감) — D-033
- [ ] 규칙 YAML 명세 스키마
- [ ] 설정 버전 체계 — 규칙 변경은 새 버전으로만 (핵심원칙 1)
- [ ] 기동 시 설정 버전 기록

### ② Strategy

- [ ] 신호 계산
- [ ] 진입 마감 시각 이후 신규 진입 차단
- [ ] 신호값 시계열 저장

### ④ Risk Mgr

- [ ] 거래당 손실 한도
- [ ] 일 손실 한도 → 도달 시 전량 청산 + 당일 잠금
- [ ] 최대 포지션 수
- [ ] 종목당 최대 노출
- [ ] 분당 최대 주문 수
- [ ] 가격 이탈 허용폭
- [ ] 손절폭 하한 강제
- [ ] 한도 위반 주문 거부

**검증.**
- 합성 시나리오 단위테스트 전량 통과
- 한도 위반 주문이 거부된다
- 일 손실 한도 도달 시 전량 청산되고 당일 잠금이 걸린다
- 임의 구간 시뮬레이션 종료 시 오버나이트 포지션이 항상 0
- **매매 경로에 LLM 호출이 없음을 코드 검색으로 확인**

---

## P4. ⑥ Watchdog + 킬스위치 봇 — 착수 순서 4

**LLM이 전부 중단돼도 동작해야 한다 (핵심원칙 3).**

- [ ] 하트비트 수집 및 누락 감지
- [ ] 강제청산 독립 집행 (Strategy·Hermes와 무관하게)
- [ ] 킬스위치 — 신규 진입 차단
- [ ] Watchdog 봇 — `/stop` `/flatten` `/status`만 처리
- [ ] **chat_id 화이트리스트** — 등록되지 않은 발신자 전부 무시 (D-027)
- [ ] 봇 토큰 유출 대비 — 화이트리스트가 유일한 방어선임을 문서화
- [ ] 서버 측 조건주문 등록 (지원 시)
- [ ] 프로세스 다운 시 재시작 전 잔고 대조 강제

**검증 (전부 통과해야 한다).**
- 장중 임의 시점에 Strategy를 강제 종료해도 Watchdog이 강제청산을 집행한다
- Hermes·Jev를 전부 중단시켜도 청산이 동작한다
- 등록되지 않은 chat_id의 `/flatten`이 무시된다
- 하트비트 중단 시 경보가 실제로 도착한다
- 재기동 시 기존 포지션을 정확히 인식한다

---

## P5. ⑦ Hermes 리포트 — 착수 순서 5

**P4(안전망)를 통과한 뒤에 설치한다.** 그전엔 읽을 로그도 없고 안전망도 없다.

### 설치와 격리 (D-034)

- [ ] `hermes` 전용 계정 생성
- [ ] 권한 설정 — 이벤트 로그 DB **읽기만**, `~/.hermes` 쓰기 허용
- [ ] `limits/` `holdout/` 접근 차단
- [ ] Hermes 설치 (원라이너)
- [ ] 모델 제공자 연결 (API 방식) + **월 비용 상한 설정**
- [ ] 서브에이전트 병렬 생성 한도 설정
- [ ] 텔레그램 연결 + **chat_id 화이트리스트** (인젝션 경로이므로 필수 — D-027)
- [ ] **매매 경로를 Hermes 크론에 걸지 않음을 확인** (systemd 전용)

### 역할

- [ ] 장전 브리핑
- [ ] 장후 복기 리포트
- [ ] 이상 탐지

**검증 (권한 시험을 통과하기 전에 규칙 제안 권한을 주지 않는다).**
- Hermes가 DB에 쓰기를 시도하면 OS 수준에서 실패한다
- Hermes가 `limits/`에 쓰려 하면 OS 수준에서 실패한다
- Hermes가 `holdout/`을 읽으려 하면 OS 수준에서 실패한다
- Hermes 환경에 증권사 API 키가 존재하지 않는다
- **Hermes를 중단시켜도 매매·청산·강제청산이 정상 동작한다**
- 등록되지 않은 chat_id의 메시지가 에이전트 컨텍스트에 들어가지 않는다

---

## P6. ③ Jev 게이트 A/B — 착수 순서 6

**Jev 없이도 전체가 돌아가는 상태에서 붙인다 (D-026).**

### 연결

- [ ] Jev 호출 인터페이스 (Vercel AI Gateway `typesafe-ai/jev`)
- [ ] 타임아웃 설정
- [ ] 타임아웃 시 폴백 (`skip_entry` / `rules_only`) — 예외가 아니라 기본 동작 경로
- [ ] 질문을 타입 있는 형태로 정의 (one-of-N / 척도 / 예-아니오)

### 보정 검증 — 진입 게이트 사용의 필수 관문 (D-035)

TypeSafe 스스로 **공표된 보정 보장이 없다**고 밝히고 있으며, 한국 소형주는 학습 분포 밖이다.
보정이 깨진 채로 임계값을 쓰면 **게이트가 없는 것보다 나쁘다.** 있다고 믿게 되기 때문이다.

- [ ] 섀도 기간에 **주문 없이 예측만 기록**
- [ ] 보정 곡선 측정 — 0.7을 준 케이스들의 실제 발생률이 70%인가
- [ ] 확률 구간별 표본 수 집계 (구간마다 충분해야 한다)
- [ ] **보정이 확인된 구간에서만 임계값 설정**
- [ ] 한국어 공시 분류 성능 측정 (보정과 별개 항목)

### A/B

- [ ] A/B 병렬 실행 — Jev 유/무 성과 분리

**검증.**
- Jev 무응답 상황에서 시스템이 설정된 폴백대로 동작한다
- **보정 곡선이 측정되어 기록되고, 측정 전에는 진입 게이트로 쓰이지 않는다**
- Jev 유/무 성과가 각각 산출되고 차이가 Jev에만 귀속됨을 로그로 추적 가능하다
- Jev 판정값이 이벤트 로그에 남아 "왜 진입했나"를 사후 재구성할 수 있다

---

## P7. 규칙 탐색 루프 — 착수 순서 7

### 가설 제안

- [ ] Hermes 가설 생성 — 조건 블록 조합의 YAML만 허용
- [ ] 조건 블록 신규 추가 요청은 승인 대기로 분리
- [ ] **비용 게이트 선행 판정** — 통과 못 하면 백테스트도 하지 않는다
- [ ] 제출 즉시 동결

### 백테스트와 검증

체결 판정은 **공용 체결 엔진(`execution/`)을 쓴다.** 페이퍼와 같은 코드여야
"동일 데이터 재실행 결과 일치" 검증이 성립한다 (D-037).

- [ ] **`가용시점` 검사** — 진입 시각보다 늦게 확정되는 블록을 참조하면 자동 기각 (D-033)
- [ ] 과거 데이터 적재 및 구간 분할 (학습 / 검증 / 홀드아웃)
- [ ] 공용 체결 엔진으로 학습 구간 백테스트
- [ ] 성과 집계 (마찰 차감 전/후 모두)
- [ ] 검증 게이트 (최소 거래 수, 워크포워드 구간, 허용 성과 저하 폭)
- [ ] 파라미터 흔들기 견고성 확인
- [ ] 수수료·거래세·슬리피지 실제 조건 반영

### 과최적화 방지

- [ ] **최종 홀드아웃 파일 권한 분리** — Hermes 접근 불가
- [ ] 홀드아웃 1회 사용 강제 (재사용 차단)
- [ ] **탈락 포함 전체 시험 횟수 기록**
- [ ] 다중검정 보정 적용 (예: Deflated Sharpe)

### 자율 배포와 한도 분리 (D-031)

- [ ] 섀도 페이퍼 트레이딩
- [ ] 검증 통과 시 **승인 없이** 새 설정 버전 생성 및 배포
- [ ] 텔레그램 **사후 통보** — 장후 발송
- [ ] 롤백 지시 처리 (이전 설정 버전으로 복귀)
- [ ] 실전 성과가 섀도 대비 기준 이하로 떨어지면 자동 비활성화

**한도 분리 — 이것이 자율 배포의 전제다.**

- [ ] 규칙 파일과 한도 파일을 분리 (`rules/` vs `limits/`)
- [ ] 에이전트 실행 계정에 `limits/` 쓰기 권한 없음
- [ ] 한도 변경은 텔레그램으로 불가. 서버 직접 접근만 허용
- [ ] Watchdog 설정도 `limits/`와 동일 취급

**검증.**
- Hermes가 홀드아웃 파일을 읽으려 하면 OS 수준에서 실패한다
- **Hermes가 한도 파일에 쓰려 하면 OS 수준에서 실패한다**
- 텔레그램으로 한도 변경을 시도하면 거부된다
- 홀드아웃 2회 사용 시도가 거부된다
- **진입 시각에 알 수 없는 정보를 참조하는 규칙이 자동 기각된다** (D-033)
- 요구 승률이 비현실적인 가설이 비용 게이트에서 탈락한다
- 조건 블록 외의 임의 코드를 담은 제안이 거부된다
- 배포된 규칙이 한도를 넘는 주문을 내면 Risk Mgr이 차단한다

---

## 사용자 정의 필요 값

`config.toml`에 `<USER_DEFINED>`로 존재하며, 값이 남아 있으면 기동이 거부된다.

### 시각
- 진입 마감 시각
- 강제청산 시각

### 리스크 한도
- 거래당 손실 한도
- 일 손실 한도
- 최대 포지션 수
- 종목당 최대 노출
- 분당 최대 주문 수
- 가격 이탈 허용폭

### Jev
- 진입 임계값
- 타임아웃
- 타임아웃 시 동작

### 유니버스
- 제외 종목 조건 목록

### 검증 게이트
- 최소 거래 수
- 워크포워드 구간
- 허용 성과 저하 폭
- 섀도 기간

### 전환
- 실전 전환 기준
- 규모 확대 기준

---

## 상시 규약

- [ ] 새 소스 파일 첫 줄에 한국어 역할 주석
- [ ] 논리 단위별 커밋
- [ ] 코드 변경 후 완료 보고 전 테스트 실행
- [ ] 결정이 생길 때마다 context-notes.md에 D-번호로 추가
- [ ] 세션 종료 시 docs/worklog.md 기록
