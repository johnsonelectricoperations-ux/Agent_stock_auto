# Checklist

> 2026-09-18 개정. 당일청산 데이트레이딩 기준.

각 단계는 **검증 통과 전까지 다음 단계로 넘어가지 않는다**.
진행하면서 체크하고, 결정이 생기면 context-notes.md에 D-번호로 덧붙인다.

---

## M0. 비용 모델 (전략 코드보다 먼저 — D-013)

- [ ] 왕복 마찰 계산기 — 수수료, 거래세 0.20%, 슬리피지
- [ ] 본전 승률 역산 `p = (f/L + 1) / (R + 1)`
- [ ] 월간 마찰 소모 시뮬레이터 (왕복 횟수 × 거래일)
- [ ] 전략 아이디어 입력 시 통과/탈락 판정하는 CLI

**검증.** 손절 1.0%, 손익비 2:1, f = 0.33% 입력 시 요구 승률 44.3%가 나온다.

---

## P0. 착수 전 확정 사항

- [x] 증권사 확정 — 한국투자증권 (D-019). 실제 수수료율 확인은 미완 (`cost.commission_*`)
- [x] 1분봉 + 일중 수급 데이터 소스 확정 — KIS Open API (D-019)
- [x] 실행 위치 확정 — GitHub Actions 크론 (D-020)
- [ ] KIS API 신청 및 앱키/시크릿 발급
- [ ] **GitHub Actions 해외 IP에서 KIS API 접속 가능 여부 검증** (D-020 선결 과제)
- [ ] 저장소 공개/비공개 상태 확인, 키는 Secrets로만 주입
- [ ] 과거 1분봉 이력 조달 깊이 확인 (관찰 단계는 전진 누적이라 치명적이지 않음 — D-019)
- [ ] 페이퍼 실행 방식 확정 (자체 시뮬레이터 / 증권사 모의투자 계좌)
- [ ] 기존 외국인 프로그램 매매 설계 문서 회수 및 재사용 범위 판단
- [ ] 리포지토리 초기화, 의존성 고정, 테스트 러너 설정

**검증.** `pytest`가 정상 종료한다. 임의 종목 1개의 1분봉을 받아온다.

---

## O. 관찰 단계 (D-017 — 전략 작성 전 필수)

목적은 **메커니즘 가설의 재료를 모으는 것**이다. 매매하지 않는다. 돈을 넣지 않는다.
직장 근무 중 개입이 불가능하므로 전 과정이 무인 자동이어야 한다 (D-020).

### O1. 수집 파이프라인 — P1 데이터 레이어의 초기 버전으로 짓는다 (D-024)

관찰용 일회성 스크립트로 만들지 않는다. 호출 주기와 분리해 작성한다.

- [ ] KIS 인증 (앱키 → 접근토큰) 및 토큰 캐싱
- [ ] 전 종목 일별 요약 수집 (분모 역할)
- [ ] 관찰 대상 선정 — **상위군 + 무작위 대조군** (D-021, 생존 편향 차단)
- [ ] 선정 근거 기록 (어떤 기준으로 누구를 골랐는지)
- [ ] 선정 종목의 1분봉 수집 (그날 하루치) — 지나가면 다시 못 구하는 데이터
- [ ] 투자자별 수급 수집 (외국인/기관/개인)
- [ ] 종목 상태 수집 (거래정지/관리종목/투자경고) — D-008
- [ ] 수집 실패 시 재시도 및 실패 로그 (조용한 누락 금지)
- [ ] API 호출 제한 실측 후 `observe.top_n`, `observe.control_n` 확정
- [ ] GitHub Actions 크론 등록 (장 마감 후 KST 기준, UTC 변환 주의)

**저장 원칙.** 파생 지표를 저장하지 않고 **원자료**를 저장한다.
지표 정의는 언제든 바뀌지만, 원자료가 있으면 다시 계산된다.

**검증.**
- 임의 거래일 1일치가 사람 개입 없이 저장소에 누적된다
- 같은 날 재실행해도 중복되지 않는다
- 대조군이 상위군과 독립적으로 추출된다
- 수집 실패가 로그에 남고 조용히 누락되지 않는다

### O2. 일일 관찰 리포트 (저녁에 5분 안에 읽을 수 있어야 함)

- [ ] 오늘 오른 종목이 **몇 시에** 오르기 시작했나 (시간대 분포)
- [ ] 그때 **누가** 사고 있었나 (수급 주체)
- [ ] 오전 상승이 오후까지 갔나, 되돌렸나
- [ ] 시가 갭 발생 종목의 이후 경로
- [ ] 마크다운 리포트로 저장소에 누적

**검증.** 리포트만 읽고 "오늘 시장에서 무슨 일이 있었나"를 재구성할 수 있다.

### O3. 관찰 종료 판정

- [ ] 최소 관찰 기간 경과 (기간은 사용자 정의 필요)
- [ ] 반복 관측된 패턴 후보 목록화
- [ ] 각 후보에 대해 "반대편에서 파는 사람은 누구이고 왜 파는가" 서술 시도
- [ ] 서술 가능한 것만 H1 가설 레지스트리로 승격

**검증.** 승격된 항목이 D-002의 메커니즘 요건(필터가 아님)을 충족한다.

---

## 트랙 1 — 파이프라인

### P1. 데이터 레이어 + 유니버스 필터

- [ ] 1분봉 수집기 (실시간 + 과거 이력)
- [ ] 일중 수급 데이터 수집기 (외국인/기관/프로그램)
- [ ] 종목 상태 수집기 (거래정지, 관리종목, 투자경고)
- [ ] 수정주가 처리 (액면분할, 무상증자, 배당락)
- [ ] 결정론적 유니버스 필터 (`universe.*`)
- [ ] 플레이스홀더 잔존 시 런타임 중단하는 설정 로더 (D-009)

**검증.**
- 임의 과거 일자 유니버스를 2회 생성했을 때 결과가 완전히 동일하다
- 해당 일자에 관리종목·거래정지였던 종목이 유니버스에 없다
- 액면분할 이력 종목 1개의 수정주가가 수기 계산과 일치한다

### P2. 신호 계산 레이어

- [ ] 일중 수급 이상 탐지
- [ ] 시간대별 정규화 (시가 변동성, 점심 소강, 마감 전 수급 — D-016)
- [ ] 신호값 시계열 저장 스키마

**검증.** 임의 종목·임의 시각의 신호값이 스프레드시트 수기 계산과 일치한다.
(LLM 미개입 구간이므로 일반 백테스트 허용 — D-004)

### P3. 룰 엔진

- [ ] 진입·청산 판정
- [ ] 포지션 사이징
- [ ] 손절 (`risk.stop_loss_pct`, 하한 1% 강제 — D-012)
- [ ] 일 왕복 횟수 상한 강제 (D-011)
- [ ] 리스크 한도 (종목당 비중, 동시 보유 수, 낙폭 중단)
- [ ] **장마감 강제청산** (D-016)
- [ ] 한도 위반 주문 거부 로직

**검증.**
- 합성 시나리오 단위테스트 통과
- 손절폭 1% 미만 설정 시 기동 거부
- 일 왕복 상한 초과 주문이 거부된다
- 임의 구간 시뮬레이션 종료 시 오버나이트 포지션 수가 항상 0
- 룰 엔진에 LLM 호출이 없음을 코드 검색으로 확인

### P4. 페이퍼 실행기

- [ ] 주문 제출·체결 시뮬레이션 (1분봉 기준)
- [ ] 상·하한가 도달 시 체결 불가 처리
- [ ] 거래정지 종목 주문 거부
- [ ] 호가 단위·슬리피지 모델 (M0 비용 모델과 동일 파라미터 사용)
- [ ] 장 마감 동시호가 체결 처리 (D-016)
- [ ] 체결·포지션 로깅

**검증.** 아래 시나리오 전량 통과.
- 상한가 종목 매수 주문 → 미체결
- 하한가 종목 매도 주문 → 미체결
- 거래정지 종목 주문 → 거부
- 강제청산이 동시호가에 걸린 경우 → 정의된 가정대로 체결
- 실현 마찰 합계가 M0 비용 모델 예측치와 오차 범위 내 일치

### P5. 에이전트 계층 + 검증 게이트

- [ ] 장전 후보 압축 프롬프트
- [ ] 장중 조건부 재평가 프롬프트 (D-014)
- [ ] JSON 출력 스키마 정의 및 강제
- [ ] 검증 게이트 — 종목코드 실재 확인
- [ ] 검증 게이트 — 수치의 DART/KRX 원본 대조
- [ ] 게이트 탈락 건 로깅
- [ ] 장중 호출 타임아웃 및 폴백 (응답 지연 시 룰 엔진 단독 동작)

**검증 (하드 게이트 — 100% 미만이면 진행 중단).**
- 존재하지 않는 종목코드 주입 시 탈락률 100%
- 틀린 재무수치 주입 시 탈락률 100%
- 에이전트 응답 타임아웃 상황에서 시스템이 정상 동작한다
- 동일 입력 3회 실행 시 출력 변동 범위를 측정해 기록

### P6. A/B 평가 하네스

- [ ] 대조군 0 실행 경로 — 코스피200 ETF 매수 후 보유 (무매매, D-018)
- [ ] 대조군 1 실행 경로 (룰 엔진 단독)
- [ ] 실험군 실행 경로 (에이전트 포함)
- [ ] 동일 기간·동일 유니버스 보장
- [ ] 성과 분리 리포트 (마찰 차감 전/후 모두 표시)

**검증.** 두 계열 성과가 각각 산출되고, 차이가 에이전트 계층에만 귀속됨을 로그로 추적 가능하다.

### M1 완료 판정

- [ ] 임의 과거 구간에 대해 파이프라인이 재현 가능하게 완주한다
- [ ] A/B 분리 측정 리포트가 나온다
- [ ] 실현 마찰이 M0 예측과 일치한다
- [ ] 실거래 주문 코드가 리포지토리에 존재하지 않음을 확인

---

## 트랙 2 — 가설 발굴 (트랙 1과 병행, 시계 별도)

### H1. 가설 레지스트리

- [ ] 가설 스키마 — 메커니즘 서술, 무효화 조건, 적용 대상, 제안 일시 (D-002)
- [ ] 사전 등록 및 동결 (동결 후 수정 불가)
- [ ] **기각 가설 포함 전량 기록** (다중검정 보정용 분모)
- [ ] 분기당 제안 상한 강제

**검증.** 동결 가설 수정 시도가 거부되고, 기각 건이 조회되며, 상한 초과 제안이 거부된다.

### H2. 제안 파이프라인

- [ ] 가설 생성 프롬프트 (메커니즘 미기재 시 자동 기각)
- [ ] 무효화 조건 미기재 시 자동 기각
- [ ] **M0 비용 게이트 통과 여부를 제안 단계에서 판정** (D-013)
- [ ] 제출 즉시 동결

**검증.** 메커니즘 없이 필터만 제출한 샘플이 자동 기각된다.
요구 승률이 비현실적인 가설이 비용 게이트에서 탈락한다.

### H3. 전진검증

- [ ] 동결 시점 이후 구간만 접근하도록 데이터 제한 (D-004)
- [ ] 최소 검증 기간 경과 전 판정 시도 차단
- [ ] 다중검정 보정 적용한 판정 로직

**검증.** 동결 시점 이전 데이터를 요청하면 예외가 발생한다.

### M3 완료 판정

- [ ] 사전 등록 가설의 전진검증 완료 및 채택/기각 판정
- [ ] 판정 근거와 기각 가설 수가 함께 기록됨

---

## M4 — 무인 운용 준비 (D-023)

**실매매 착수의 필수 선결 조건이다. 하나라도 미달이면 실매매하지 않는다.**
M3까지가 "기대값이 있는가"의 검증이라면, 여기는 "사람 없이 돌려도 되는가"의 검증이다.

- [ ] **KIS 자동 손절 예약 지원 여부 확인** — 미지원이면 실매매 설계 재검토
- [ ] 주문 체결과 동시에 증권사 측 손절 예약 등록
- [ ] 상시 실행 환경 구축 및 이전 (D-022)
- [ ] 하트비트 감시 및 운용자 단말 통지
- [ ] 포지션·주문 상태 영속화, 재기동 시 복구
- [ ] 원격 킬스위치
- [ ] 무인 운용 자본 상한 강제 (`ops.max_capital`)
- [ ] 일일 운용 리포트 자동 발송

**검증 (전부 통과해야 한다).**
- 장중 임의 시점에 프로세스를 **강제 종료**시켰을 때 포지션이 방치되지 않는다
- 강제 종료 후 재기동 시 기존 포지션을 정확히 인식한다
- 네트워크 단절 상황에서도 증권사 측 손절이 유효하다
- 킬스위치 작동 후 신규 진입이 차단된다
- 하트비트 중단 시 통지가 실제로 도착한다

---

## 실매매 전환 전 확인 (미해결)

- [ ] 사용 증권사 API 약관상 자동매매 허용 범위
- [ ] 국내 알고리즘 매매 관련 규정
- [ ] M0~M4 전 단계 완료 확인
- [ ] 운용 자본 결정 — 없어져도 생활에 지장 없는 금액

---

## 상시 규약

- [ ] 새 소스 파일 첫 줄에 한국어 역할 주석
- [ ] 논리 단위별 커밋
- [ ] 코드 변경 후 완료 보고 전 테스트 실행
- [ ] 결정이 생길 때마다 context-notes.md에 D-번호로 추가
