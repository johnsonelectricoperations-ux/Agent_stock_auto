# Worklog

## 2026-09-24 (4차) — 외부 도구 실사, AWS 확정

- **목표.** Hermes와 Jev가 실제로 어떤 물건인지 확인하고 설계에 반영한다.

- **확인 방법.** 웹 검색과 공식 문서. 다만 이 컨테이너는 외부 접속이 대부분 막혀
  검색 결과 스니펫과 GitHub 저장소 정도만 읽을 수 있었다. 재확인 여지가 있다.

- **Hermes Agent (D-034).** 실재. MIT, 2026-02 출시, 원라이너 설치, "$5 VPS"급.
  - **모델 제공자 무관** → 로컬 GPU 불요. 명세 10절 미결 해소.
  - 범용 에이전트다. 스킬 자율 생성, 내장 크론, 메시징, 서브에이전트 병렬.
  - **기존 문서의 "읽기 전용" 표현이 틀렸다.** `~/.hermes`에 계속 쓴다.
    "자기 영역 밖에 쓰지 못한다"로 정정.
  - 매매 경로를 Hermes 크론에 걸면 LLM 정지 시 안전장치도 멈춘다 → systemd 전용.
  - 텔레그램 통합 = 인젝션 경로. chat_id 화이트리스트가 필수가 됨.

- **Jev (D-035).** 실재. **2026-09-15 출시. 9일 됐다.**
  - 게이트웨이로 대기열 우회 가능 → 접근 불가 우려는 해소.
  - 속도가 LLM 대비 2자릿수 빠름 → 진입 게이트로는 오히려 적합.
  - **그러나 D-026은 유지.** 근거가 바뀌었다.
    TypeSafe 스스로 "공표된 보정 보장 없음, 고객 데이터로 테스트하라"고 한다.
    분포 이동 하 보정 유지는 열린 질문. 한국 소형주는 명백히 분포 밖이다.
  - 보정 곡선 측정을 진입 게이트 사용의 **선행 관문**으로 체크리스트에 박았다.

- **AWS (D-036).** 서울 리전, 작은 CPU 인스턴스, SSM(인바운드 0), 계정 분리,
  백업, 서버 밖 감시자, 비용 알람.

- **다음 할 일.**
  1. **작은 인스턴스로 증권사 API 접속 확인** — AWS 서울이 데이터센터 대역이라 미검증.
     이게 안 되면 나머지 전부 무의미하다. 소액으로 먼저 확인.
  2. 증권사 서버 측 조건주문 지원 여부 (D-030)
  3. 백테스트용 과거 분봉 조달 경로
  4. SQLite 이벤트 로그 스키마 설계 (증권사와 무관하게 지금 가능)

- **주의.** 도구 실사는 2026-09-24 기준이다. Jev는 출시 9일차라 API·가격·정책이
  바뀔 수 있다. Hermes도 외부 의존이므로 유지보수 상태를 계속 봐야 한다.

---

## 2026-09-24 (3차) — 종목 선정 3층 분리, 고정 코호트 추가

- **목표.** "종목을 어떻게 선정해서 자료를 쌓는가"에 답하면서 look-ahead 함정을 막는다.

- **완료.** 테스트 57개 → 63개.
  - **D-033** 선정을 A층(장전)·B층(장중)·C층(장후)으로 분리.
    C층은 답을 보고 고르는 것이라 그 자체가 미래 정보다.
    C층 데이터를 진입 조건에 쓰면 백테스트만 되는 규칙이 나온다.
    C층으로는 B층 조건을 **발굴**만 한다.
  - 조건 블록에 `가용시점` 필드. 백테스트가 진입 시각보다 늦게 확정되는
    블록 참조를 자동 기각한다. D-004의 실제 구현 방식이다.
  - **D-033-1** 고정 코호트 추가. 대조군은 매일 바뀌어 시계열이 끊기므로
    기준선이 못 된다. 1회 추첨 후 고정하고, 빠진 종목은 대체하지 않는다.
  - `selection.py`에 `draw_cohort()` 추가, `select_candidates()`가 코호트 필수.
  - `config.toml`에 `cohort_n`, `cohort_seed` 추가.

- **AWS 관련 논의.** 서버는 AWS로 결정. 아직 문서에 반영하지 않았다.
  - 리전 서울 고정 (해외 IP 제한 회피, 주문 지연 = 슬리피지)
  - SSH 포트 미개방 (SSM Session Manager), 아웃바운드도 제한
  - 계정 분리로 D-028/D-031을 OS 수준에서 구현
  - 원자료 백업 필수 (다시 못 구한다)
  - 서버 밖 감시자 필요 (서버가 죽으면 Watchdog도 죽는다)
  - **Hermes 모델을 API로 붙일지 확정 필요** — 인스턴스 사양이 갈린다

- **다음 할 일.**
  1. AWS 인프라 항목을 checklist P1에 반영 (미완)
  2. 증권사 서버 측 조건주문 지원 여부 (D-030)
  3. 백테스트용 과거 분봉 조달 경로
  4. 작은 인스턴스로 증권사 API 접속 확인 — 서버 본격 구축 전에

---

## 2026-09-24 (2차) — 자율성 범위 확정, 수집 범위 재정의

- **목표.** "에이전트가 규칙을 스스로 설계·검증·실행한다"를 설계에 반영한다.

- **확정된 방향 (운용자 결정).**
  - 자율성 = 규칙 배포까지. 주문은 결정론적 코드 유지.
  - 검증 = 독립 채점 유지. 홀드아웃은 Hermes 접근 불가.

- **완료.** 테스트 57개 통과.
  - **D-031** 핵심원칙 1 개정. 승인 제거, 안전을 하드 리밋으로 이동.
    한도·Watchdog 설정은 사람만, 파일 권한으로 강제. 텔레그램으로도 변경 불가.
    텔레그램 역할이 승인 → 사후 통보 + 롤백 지시로 바뀜.
  - **D-032** 수집 범위를 "규칙이 쓰는 것" → "규칙이 만들어질 수 있는 것"으로 재정의.
    원자료만 저장, 분모 함께 수집, 체결 품질 실측.
    전 종목 일봉·수급 + 선정 종목 분봉·호가 구성.
  - `collector/selection.py` 보류 해제. 대조군 목적으로 부활.
  - `config.toml`의 성격을 "사람 전용 한도 파일"로 명확히 함.

- **다음 할 일.** P1 착수 전 확정 사항. 여전히 아래 셋이 막고 있다.
  1. **증권사 서버 측 조건주문 지원 여부** (D-030)
  2. **백테스트용 과거 분봉 조달 경로** — D-032로 중요도가 더 올라갔다.
     에이전트 탐색 공간의 상한을 결정한다.
  3. 상시 실행 환경 결정

- **주의.**
  - 자율 배포는 **한도 분리가 구현된 뒤에만** 켜야 한다.
    한도 없이 자율 배포를 켜면 방어선이 전혀 없다. P7 체크리스트의 순서를 지킬 것.
  - 전 종목 분봉은 호출 제한상 불가능하다는 가정 위에 선정 구조를 세웠다.
    실측 후 가정이 틀리면 재검토한다.

---

## 2026-09-24 — 설계 전면 개정 (에이전트 기반 7프로세스)

- **목표.** 운용자가 제공한 새 설계 명세를 기준으로 문서를 다시 쓴다.

- **완료.** 테스트 57개 통과. plan.md / checklist.md / context-notes.md 전면 재작성.
  - 구조가 바뀌었다. 관찰 단계 중심 → 7프로세스 + 권한 분리.
  - 비용 게이트(`costgate/`)와 설정 로더(`appconfig.py`)는 그대로 승계.
  - `config.toml`을 명세 9절 파라미터로 재작성, `<USER_DEFINED>` 표기로 통일.
  - 명세에 없던 위험 5건을 D-026~D-030으로 기록했다.

- **기존 코드 처리.** 지우지 않았다. D-025에 승계/보류/폐기예정을 표로 명시.
  - 보류: `collector/selection.py` (관찰 단계 폐기로 적용 대상 없음)
  - 폐기 예정: `.github/workflows/` (상시 환경 이전 후 제거)

- **다음 할 일.** P1 착수 전 확정 사항. 특히 아래 셋이 다른 모든 작업을 막는다.
  1. **증권사 서버 측 조건주문 지원 여부** (D-030) — 무인 운용 가부를 좌우
  2. **백테스트용 과거 분봉 조달 경로** — 규칙 탐색 루프 전체의 전제
  3. 상시 실행 환경 결정 — 7프로세스를 어디서 돌릴 것인가

- **주의.**
  - Jev는 얼리 액세스 미확보 상태다. 없는 상태를 정상으로 보고 설계했다 (D-026).
  - Hermes Agent의 실제 가용성·유지보수 상태를 확인하지 못했다.
  - `collector/kis_client.py`의 엔드포인트는 여전히 미검증이고, 증권사가
    확정되지 않아 보류 상태다.

---

## 2026-09-21 (2차) — API 비의존 코어 구현

- **목표.** 운용자가 KIS API를 신청하는 동안, 키 없이 검증 가능한 부분을 완성한다.

- **원칙.** KIS 응답 형식에 의존하는 코드와 의존하지 않는 코드를 분리한다.
  형식은 실호출해봐야 알 수 있으므로 그 부분은 얇게 두고 나머지를 단단히 만든다.

- **완료.** 테스트 56개 통과 (이전 29개 → 56개).
  - `appconfig.py` + `config.toml` — 미정 값이 남으면 기동 거부 (D-009).
    모드별로 필요한 키만 검사한다. 전부 검사하면 관찰조차 시작 못 한다.
  - `collector/selection.py` — 상위군 + 무작위 대조군 (D-021).
    대조군 0을 코드에서 거부한다. 날짜를 난수 씨앗으로 써서 재현 가능하다.
  - `collector/storage.py` — 원자적 쓰기, 중복 방지, 실패 로그.
    반쯤 쓰인 파일이 '이미 수집함'으로 오인되는 것을 막는다.

- **다음 할 일.** 키가 도착하면
  1. 접속 확인 워크플로 실행 → 해외 IP 가부 판정
  2. 통과 시 KIS 응답 형식 실측 (분봉 조회 깊이, 호출 제한, 수급 확정 시각)
  3. 실측 결과로 `observe.top_n`, `observe.control_n` 확정 → config.toml 기입
  4. 수집 실행기 작성 → 위 세 모듈을 엮는다

- **주의.**
  - `collector/kis_client.py`의 엔드포인트·tr_id는 여전히 **미검증**이다.
  - **KIS 자동 손절 예약 지원 여부**를 API 문서에서 확인해야 한다 (D-023).
    미지원이면 M4 설계가 바뀐다.

---

## 2026-09-21

- **목표.** 최종 목표(매일 무인 자동매매)를 기준으로 전체 설계 재검토.

- **완료.** D-021~D-024 기록, plan.md에 종점 아키텍처·안전 원칙 추가,
  checklist에 M4(무인 운용 준비) 신설.
  - **D-023이 가장 중요하다.** 장중 시스템 정지 시 포지션 방치 위험이 설계에서
    통째로 빠져 있었다. 직장 근무로 인지가 불가능하고, 미청산 포지션은
    당일청산 전제를 무너뜨린다. 증권사 측 손절 예약을 실매매 선결 조건으로 세웠다.
  - D-022로 D-020을 개정. Actions는 관찰 전용, 실매매는 상시 환경.
  - D-021로 수집에 무작위 대조군 추가 (생존 편향).
  - D-024로 수집기를 P1 데이터 레이어 초기 버전으로 규정.

- **다음 할 일.** 2026-09-18 항목과 동일하다. KIS 접속 확인이 여전히 병목이다.
  추가로 **KIS 자동 손절 예약 지원 여부**를 확인해야 한다. 미지원이면 M4 설계가 바뀐다.

- **주의.** 코드는 이번 세션에서 변경하지 않았다. 문서만 개정했다. 테스트 29개 통과 유지.

---

## 2026-09-18

- **목표.** 폴더 내용 파악, 에이전트 온라인/오프라인 방향 결정, 착수 지점 확정.

- **완료.**
  - 기존 문서 3종(plan/checklist/context-notes) 파악. 코드는 없던 상태였다.
  - 운용자가 일단타 경험이 없음을 확인 → 전략 작성 전 **관찰 단계 신설** (D-017).
  - 성과 대조군에 "무매매(인덱스 보유)" 추가 (D-018).
  - 데이터 소스 KIS Open API 확정 (D-019). 운용자가 한투 계좌 보유.
  - 실행 위치 GitHub Actions 크론 확정 (D-020). 운용자 직장 근무로 장중 개입 불가.
  - **M0 비용 게이트 구현 완료.** `costgate/` + CLI. 테스트 29개 통과.
    plan.md의 요구 승률 표 9칸과 D-011의 월간 마찰 표를 재현하는지 검증했다.
  - KIS 접속 확인 스크립트와 수동 워크플로 작성
    (`.github/workflows/kis-connectivity-check.yml`).

- **에이전트 온라인/오프라인 결론.**
  D-014가 이미 정한 대로 간다. 실행 경로(손절·청산·사이징)는 오프라인 결정론적 코드,
  판단 보조 경로(장전 압축·장중 재평가·장후 복기)만 온라인 저빈도 호출.
  보유시간이 수십 분이라 LLM 왕복 지연 1~10초는 무시 가능하다.

- **다음 할 일 (운용자 선결 작업이 먼저다).**
  1. KIS Open API 신청 → 앱키/시크릿 발급
  2. 저장소 Settings → Secrets and variables → Actions에
     `KIS_APP_KEY`, `KIS_APP_SECRET` 등록
  3. Actions 탭에서 "KIS 접속 확인" 워크플로 수동 실행
  4. 결과 공유 → 통과 시 O1 수집기 착수, 실패 시 집 PC 또는 한국 리전 VPS로 전환

- **주의.**
  - Actions 러너가 해외 IP라 KIS가 차단할 가능성이 있다. **미검증 상태다.**
  - 이 컨테이너는 프록시가 외부 접속을 막아 KIS를 직접 테스트하지 못했다.
    `collector/kis_client.py`의 엔드포인트와 tr_id는 KIS 공식 문서와 대조가 필요하다.
  - 저장소 공개/비공개 상태를 확인할 것. 수집 데이터에 계좌 정보가 섞이면 안 된다.
  - 관찰 단계 최소 기간, 수수료율 등 미정 파라미터는 여전히 플레이스홀더다 (D-009).
  - 실거래는 M3 이후 별도 판단이며 현재 범위 밖이다.
