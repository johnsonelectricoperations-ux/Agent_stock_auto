# 에이전트 AI 기반 주식 자동매매 시스템 — 플랜

> 2026-09-24 전면 개정. 에이전트 기반 7프로세스 구조로 재설계.
> 이전 설계(관찰 단계 중심, GitHub Actions 실행)는 폐기했다. 승계·폐기 내역은 context-notes.md D-025 참조.

---

## 1. 목적과 범위

코스피·코스닥 **당일청산 데이트레이딩** 시스템을 만든다.
보유 수십 분~장마감, 오버나이트 없음, 초단위 스캘핑 제외.

**1차 마일스톤은 룰엔진 + 페이퍼 트레이딩이다.** 실전 주문은 그 이후 별도 판단이다.

운용자가 직장 근무 중이라 장중 개입이 불가능하다. 이 제약이 설계 전반을 규정한다.

### 검토 도구

| 도구 | 성격 | 역할 | 실사 결과 |
|---|---|---|---|
| Hermes Agent | Nous Research, MIT, 2026-02 출시 | 운영·관제 + 규칙 탐색 연구원 | 확인됨. 모델 제공자 무관이라 GPU 불요 (D-034) |
| Jev | TypeSafe AI, **2026-09-15 출시** | 공시 분류, 진입 전 게이트 | 확인됨. **도메인 보정 미검증** (D-035) |
| 텔레그램 | 메신저 | 리포트·통보, 킬스위치 | Hermes에 내장. Watchdog 봇만 별도 구현 |

---

## 2. 핵심 원칙 (절대 규칙)

1. **규칙은 자율, 한도는 사람.** (2026-09-24 개정 — D-031)
   에이전트가 규칙을 설계·검증·배포까지 자율 수행한다. 사람 승인은 요구하지 않는다.
   대신 **하드 리밋과 Watchdog 설정은 사람만 바꿀 수 있다.** 파일 권한으로 강제한다.
   규칙 변경은 여전히 **새 설정 버전으로만** 반영되고 실행 중 즉석 변경은 없다.
2. **주문 경로에 LLM을 넣지 않는다.**
   Hermes를 포함한 어떤 LLM도 주문하지 않는다. 결정론적 코드만 주문한다.
3. **킬스위치·강제청산은 LLM이 전부 중단돼도 동작한다.**
   Watchdog은 LLM과 무관한 독립 경로다.
4. **검증되지 않은 파라미터는 `<USER_DEFINED>`로 둔다.**
   임의 기본값 금지. 플레이스홀더가 남아 있으면 기동을 거부한다.

### 원칙 1과 2의 관계 — 자율성은 어디까지인가

```
에이전트 자율          결정론적 코드          사람 전용
─────────────         ─────────────         ─────────────
규칙 설계              규칙 집행              하드 리밋
가설 생성              주문 제출              Watchdog 설정
지표 정의              손절 실행              킬스위치
배포 판단              강제청산
```

에이전트가 벽돌을 직접 쌓지 않는다고 해서 그 건물이 에이전트 설계가 아닌 것은 아니다.
규칙은 100% 에이전트가 만들고, 코드는 그것을 판단 없이 집행한다.

주문 경로를 코드로 두는 이유는 자율성을 줄이기 위해서가 아니다.
Hermes는 외부에서 오는 뉴스·공시 텍스트를 읽는다. 주문 권한이 붙으면
**조작된 텍스트가 주문을 유발할 수 있다.** 읽기 전용일 때 최악은 잘못된 리포트지만,
주문 권한이 있으면 최악은 남이 내 돈으로 내는 주문이다.

---

## 3. 구성요소와 권한 경계

권한 분리가 이 설계의 핵심이다. **약속이 아니라 구조로 막는다.**

| 구성요소 | 할 수 있는 것 | 할 수 없는 것 |
|---|---|---|
| **Hermes** | 이벤트 로그 DB 읽기, `~/.hermes` 쓰기, 리포트 생성, 규칙 YAML 제안·배포 | 주문, API 키 접근, DB 쓰기, 최종 홀드아웃 접근, **한도 변경** |
| **Jev** | 확률 판정 반환 | 주문, 규칙 변경, 탐색 |
| **Executor** | 주문·체결, **증권사 API 키 단독 보유** | 규칙 판단 |
| **Watchdog** | 강제청산, 킬스위치 | 신규 진입 |
| **Hermes 봇** | 대화·리포트·규칙 승인 요청 | 즉시 주문 |
| **Watchdog 봇** | `/stop` `/flatten` `/status` | 그 외 전부 |

**Hermes가 침해돼도 주문은 불가능해야 한다.** API 키를 Executor만 들고 있는 이유다.

### Hermes의 권한 경계 (D-034)

Hermes는 범용 에이전트라 자기 영역에는 계속 써야 한다.
**"읽기 전용"이 아니라 "자기 영역 밖에 쓰지 못한다"가 정확한 표현이다.**

```
~/.hermes/          쓰기 허용    메모리·스킬·설정
이벤트 로그 DB       읽기만       OS 권한으로 강제
limits/  holdout/   접근 불가    OS 권한으로 강제
```

**매매 경로 스케줄링에 Hermes 내장 크론을 쓰지 않는다.**
장전 점검·진입 마감·강제청산이 Hermes 크론에 걸리면 LLM이 멈출 때 안전장치도 함께 멈춘다.

```
매매 경로     →  systemd
Hermes 자기 작업 (리포트, 규칙 탐색)  →  Hermes 크론 허용
```

**텔레그램 통합은 프롬프트 인젝션 경로다.** 수신 메시지가 에이전트 컨텍스트에 들어간다.
chat_id 화이트리스트(D-027)가 권장이 아니라 필수다.

---

## 4. 프로세스 구성

```
[매매 경로 — LLM 없음]
  ① Collector   시세·호가·수급·공시 수집
        ↓
  ② Strategy    룰엔진 → 신호
        ↓
  ③ Jev Gate    확률 판정. 타임아웃 시 폴백      ← 선택적 부품 (D-026)
        ↓
  ④ Risk Mgr    하드 리밋. 통과 못 하면 주문 불가
        ↓
  ⑤ Executor    주문·체결. API 키 단독 보유

[감시 경로]
  ⑥ Watchdog    하트비트, 강제청산, 킬스위치      ← LLM 무관

[운영 경로]
  ⑦ Hermes      읽기 전용

공통: 모든 이벤트 → 이벤트 로그 DB (SQLite, WAL)
```

③은 **없어도 전체가 돌아가야 한다.** Jev 얼리 액세스가 확보되지 않을 수 있기 때문이다 (D-026).

---

## 4-1. 수집 범위 — 에이전트의 탐색 공간을 결정한다 (D-032)

에이전트가 규칙을 자율 설계하므로, **내일 어떤 조건 블록을 제안할지 오늘 알 수 없다.**
제안 시점에 과거 데이터가 없으면 백테스트를 못 하고, 백테스트를 못 하면 검증도 못 한다.

> **수집하지 않은 데이터는 영원히 쓸 수 없다. 수집 범위가 곧 탐색 가능 공간의 상한이다.**

### 세 가지 원칙

**① 원자료를 저장하고 파생 지표는 저장하지 않는다.**
지표 정의는 에이전트가 바꾼다. 원자료가 있으면 언제든 다시 계산되지만,
지표만 저장하면 정의가 바뀌는 순간 과거 전체가 쓸모없어진다.

**② 분모를 함께 모은다.**
"이 조건에서 올랐다"만으로는 규칙이 검증되지 않는다. "같은 조건인데 안 오른 경우"가 있어야 한다.
오른 종목만 모으면 에이전트가 반드시 생존 편향에 빠진 규칙을 만들어낸다.

**③ 체결 품질을 실측한다.**
주문 의도가와 실제 체결가의 차이를 기록한다. 슬리피지 가정이 틀리면 백테스트 전체가 무의미해진다.

### 수집 구성

| 대상 | 범위 | 역할 | 무게 |
|---|---|---|---|
| 일봉 + 투자자별 수급 | **전 종목** | 분모 | 가볍다 |
| 분봉 + 호가 | **선정 종목** | 상세 분석 | 무겁다 |
| 공시 | 전 종목 | Jev 입력, 이벤트 규칙 재료 | 보통 |
| 종목 상태 | 전 종목 | 거래정지·관리종목·투자경고 | 가볍다 |
| 체결 기록 | 자기 주문 | 슬리피지 실측 | 가볍다 |

전 종목 분봉은 API 호출 제한상 불가능하다. 따라서 선정이 필요하다.

### 선정은 3층으로 나뉜다 (D-033)

**섞으면 백테스트만 되는 규칙이 나온다.** 사후 선정은 답을 보고 고르는 것이라 그 자체가 미래 정보다.

| 층 | 시점 | 무엇으로 정하나 | 용도 |
|---|---|---|---|
| **A. 사전 유니버스** | 장 시작 전 | 전일까지 확정된 정보 (시총, 평균 거래대금, 상장일, 종목상태) | 매매 모집단 |
| **B. 장중 동적 편입** | 장중 | 그 시점에 계산 가능한 것 (09:15까지 누적 거래대금 등) | **실제 매매** |
| **C. 사후 수집 선정** | 장 마감 후 | 당일 결과 | **분석 재료** |

```
[08:30]  A층 — 전일 데이터로 유니버스 확정
[09:00~] B층 — 조건 충족 시 동적 편입 → 매매는 여기서만
[15:40]  C층 — 당일 결과로 수집 대상 선정 → 내일 이후 분석 재료
```

**C층 데이터를 진입 조건에 직접 쓰지 않는다.** C층으로 찾는 것은
"이 종목이 움직이기 시작하기 전에 장중에 감지할 수 있는 신호가 있었나"이다.
즉 C층 데이터로 **B층 조건을 발굴**한다.

### C층 수집 대상 세 종류

```
상위군       매일 새로 (그날 움직인 것)   → 신호 후보
대조군       매일 무작위                  → 분모. 빠지면 생존 편향
고정 코호트  1회 추첨 후 고정             → 기준선. 없으면 특이값 판단 불가
```

**고정 코호트에서 빠진 종목은 대체하지 않는다.** 기록만 한다.
대체하면 살아남은 종목만 남아 또 생존 편향이 생긴다.

선정 결과는 **어떤 기준으로 누구를 골랐는지와 함께** 기록한다.
나중에 에이전트가 "그날 왜 이 종목만 있나"를 알 수 있어야 한다.

### 조건 블록의 `가용시점` — look-ahead를 구조로 막는다

조건 블록마다 "언제 계산 가능한가"를 명시하고, **백테스트 엔진이 이를 검사한다.**

```yaml
- 블록: 전일_평균거래대금_대비      # 가용시점: 장전
- 블록: 09시30분까지_누적거래대금    # 가용시점: 09:30 이후
- 블록: 당일_종가등락률             # 가용시점: 장마감 → 진입 조건 사용 불가
```

진입 시각보다 늦게 확정되는 블록을 참조하는 규칙은 **자동 기각**이다.
에이전트가 아무리 그럴듯하게 조합해도 통과하지 못한다.
"조심한다"로는 막을 수 없기 때문에 구조로 막는다.

## 5. 하루 운영 흐름

| 시점 | 동작 | 실패 시 |
|---|---|---|
| 장전 | 연결·잔고·설정 버전 점검 | **당일 신규 진입 없음** |
| 장중 | 자동 매매 | 아래 장애 대응표 |
| 진입 마감 이후 | 청산만 허용 | — |
| 장마감 | Watchdog이 강제청산 독립 집행 | — |
| 장후 | 증권사 잔고 ↔ 내부 기록 대조 | **불일치 시 다음 날 매매 잠금** |
| 장후 | 복기 리포트 (Hermes) | 리포트 실패는 매매와 무관 |

규칙 승인 요청은 **장후에만** 발송한다. 장중에는 운용자가 볼 수 없다 (D-029).

---

## 6. 장애 대응

| 상황 | 동작 |
|---|---|
| Jev 무응답 | 설정된 폴백 (`skip_entry` / `rules_only`) |
| 시세 끊김 | 신규 진입 중단, **청산 로직은 유지** |
| 증권사 API 끊김 | 즉시 경보. 서버 측 조건주문에 의존 |
| 일 손실 한도 도달 | 전량 청산 + 당일 잠금 |
| 주문 꼬임 | 주문별 고유 ID, 증권사 기준 재동기화 |
| 프로세스 다운 | 하트비트 누락 경보, 재시작 전 잔고 대조 |

**최후의 안전망은 증권사 서버 측 조건주문(스탑로스)이다.**
Watchdog도 프로세스라 죽을 수 있다. 내 서버가 전부 죽어도 증권사 서버는 살아 있다.
→ **서버 측 조건주문 지원 여부가 증권사 선택의 1순위 기준이다** (D-030).

---

## 7. 비용 게이트 — 규칙의 최소 자격

규칙 탐색 루프가 제안하는 모든 규칙은 **백테스트 이전에** 이 게이트를 통과해야 한다.
과거 성적이 좋아도 마찰을 넘지 못하면 구조적으로 불가능하다.

왕복 마찰 `f = 매수수수료 + 매도수수료 + 거래세 0.20% + 슬리피지`
본전 승률 `p = (f/L + 1) / (R + 1)` (L = 손절폭, R = 손익비)

f ≈ 0.33% 기준 요구 승률.

| 손절폭 | 1.5:1 | 2:1 | 3:1 |
|---|---|---|---|
| 0.5% | 66.4% | 55.3% | 41.5% |
| 1.0% | 53.2% | 44.3% | 33.3% |
| 2.0% | 46.6% | 38.8% | 29.1% |

**거래세 0.20%는 고정비다.** 손절폭을 좁힐수록 마찰의 상대 비중이 커져 요구 승률이 급등한다.
촘촘히 끊는 쪽이 안전해 보이지만 구조적으로 전략을 죽인다.

일 왕복 횟수별 월 마찰 소모 (20거래일, 매회 전량 투입, f = 0.33%).
1회 6.6% / 2회 13.2% / 3회 19.8% / 5회 33.0%

구현은 `costgate/`에 있고 테스트로 위 표를 고정해 두었다.

---

## 7-1. 공용 체결 엔진 (D-037)

체결 판정은 `execution/`에 **하나만** 두고 세 모드가 공유한다.

```
            체결 엔진 (하나)
                 │
   ┌─────────────┼─────────────┐
백테스트       페이퍼        섀도
과거 분봉      실시간 분봉    실시간 분봉
```

모드별로 따로 만들면 명세 9절의 "페이퍼와 동일 데이터 재실행 결과 일치" 검증이 성립하지 않는다.
성과가 갈릴 때 전략 탓인지 체결 가정 탓인지 구분할 수 없다.

**구현된 방어 장치.**

| 장치 | 막는 것 |
|---|---|
| 체결 봉 > 신호 봉 강제 | 신호 확정 봉의 종가로 체결하는 백테스트 최대 빈발 버그 |
| 비용을 `costgate`에서 재사용 | 백테스트 마찰과 비용 게이트 수치가 어긋나는 것 |
| 마찰 실현값 ↔ 예측값 대조 테스트 | 두 모듈이 조용히 갈라지는 것 |
| 매수 올림 / 매도 내림 반올림 | 체결마다 성과가 부풀려져 누적되는 것 |
| 호가 단위 표를 설정에서 받음 | 검증 안 된 규정 값이 조용히 들어가는 것 |

**정의된 가정.** 장 마감 동시호가는 해당 봉의 종가로 단일가 결정된다고 본다. 검증 대상이다.

**남은 부분.** 체결 판정은 끝났다. 백테스트 엔진에는 규칙 표현(P3),
가용시점 검사(D-033), 과거 데이터 적재, 성과 집계가 남아 있다.

## 7-2. 성과 측정 기준 (D-038)

성과 리포트는 **세 계열을 나란히** 산출한다.

```
대조군 0   코스피200 ETF 매수 후 보유 (무매매)   ← 진짜 기준선
대조군 1   룰 엔진 단독 (에이전트 없음)
실험군     에이전트 포함
```

A/B만으로는 "에이전트가 기여했나"에만 답한다.
**"이 시스템을 만든 게 나았나"는 대조군 0이 있어야 답할 수 있다.**
몇 달 고생한 결과가 인덱스를 그냥 들고 있는 것만 못하면 안 만드는 게 나았던 것이다.

## 8. 규칙 자동 탐색 루프

```
Hermes 가설 생성 (YAML 명세)
        ↓
비용 게이트            ← 통과 못 하면 백테스트도 하지 않는다
        ↓
백테스트 (학습 구간)
        ↓
검증 게이트
        ↓
최종 홀드아웃 (1회)    ← Hermes 접근 불가. 파일 권한으로 분리
        ↓
섀도 페이퍼 트레이딩
        ↓
실전 배포 (새 설정 버전)  ← 에이전트 자율 (D-031)
        ↓
텔레그램 통보            ← 사후. 이의 있으면 롤백 지시
```

배포는 **하드 리밋 안에서만** 일어난다. 에이전트는 한도 파일에 쓰기 권한이 없다.
한도 변경은 텔레그램으로도 불가능하며 서버 직접 접근을 요구한다.
한도가 마지막 방어선인데 그 변경 경로를 메신저에 열어두면 방어선이 메신저 보안 수준으로 내려간다.

### 제약

- 규칙은 코드가 아니라 **사전 정의된 조건 블록 조합의 YAML 명세**로만 제안한다.
- **조건 블록 신규 추가는 승인 사항이다.** Hermes가 임의로 블록을 늘릴 수 없다.
- 실전 규칙 성과가 섀도 대비 기준 이하로 떨어지면 **자동 비활성화**한다.

### 과최적화 방지

- 최종 홀드아웃은 Hermes가 **파일 권한 수준에서** 접근 불가하다. 코드 약속으로는 부족하다.
- **탈락 포함 전체 시험 횟수를 기록**하고 다중검정 보정을 적용한다 (예: Deflated Sharpe).
  분모를 모르면 우연과 실력을 구분할 수 없다.
- 파라미터를 흔들어 견고성을 확인한다. 특정 값에서만 되는 규칙은 기각이다.
- 수수료·거래세·슬리피지를 실제 조건으로 반영한다.

**목표는 "과거 최고 성과"가 아니라 "검증을 통과한 견고한 규칙"이다.**

---

## 9. 실전 전환 단계

| 단계 | 검증 내용 |
|---|---|
| 1. 페이퍼 | 동일 데이터 재실행 결과 일치, 장애 시나리오 강제 재현 |
| 2. 소액 실전 | 페이퍼 대비 슬리피지·체결률 차이 측정 |
| 3. 규모 확대 | 전환 기준 `<USER_DEFINED>` |

---

## 10. 마일스톤

| | 내용 | 완료 기준 |
|---|---|---|
| M0 | 비용 게이트 | 임의 가정에 대해 요구 승률 즉시 산출 **(완료)** |
| M0-1 | **공용 체결 엔진** | 상·하한가·거래정지·동시호가 시나리오 통과. 실현 마찰이 비용 게이트 예측과 일치 **(완료)** |
| M1 | ① Collector + ⑤ Executor(페이퍼) | 하루치 수집·기록이 무인으로 완주. 주문은 전부 가짜 |
| M2 | ② Strategy + ④ Risk Mgr | 신호 생성과 하드 리밋 차단이 합성 시나리오에서 동작 |
| M3 | ⑥ Watchdog + 킬스위치 봇 | **강제 종료 시험에서 포지션이 방치되지 않음** |
| M4 | ⑦ Hermes 리포트 | 읽기 전용임이 파일 권한으로 확인됨 |
| M5 | ③ Jev 게이트 A/B | 유/무 성과가 분리 측정됨 |
| M6 | 규칙 탐색 루프 | 사전 등록 가설 1건의 전 과정 완주 |

**실전 전환은 M6 이후 별도 판단이며 이 문서의 범위가 아니다.**

---

## 11. 범위 밖 (명시적 제외)

- 실거래 주문 — M6 평가 완료 전까지 코드에 존재하지 않는다
- 오버나이트 포지션
- 초~수십초 스캘핑 — 마찰 구조상 배제
- 미국 시장 — 이식하지 않는다. 별도 재설계 사안

---

## 12. 미결 사항

| 항목 | 왜 중요한가 |
|---|---|
| **증권사 API — 서버 측 조건주문 지원 여부** | 무인 운용의 최후 안전망. 증권사 선택 1순위 기준 (D-030) |
| 증권사 API — 웹소켓 지원 | ① Collector 구조를 결정한다 |
| **AWS 서울 IP를 증권사가 막는지** | 데이터센터 대역이라 개인 회선과 다를 수 있다. 본격 구축 전 확인 (D-036) |
| **백테스트용 데이터 범위** | 규칙 탐색 루프 전체의 전제. 기간·분봉 단위·프로그램 매매 수급 포함 여부 |
| **Jev 도메인 보정 검증** | 한국 소형주는 학습 분포 밖이다. 보정 미확인 상태로 임계값을 쓰면 게이트가 없는 것보다 나쁘다 (D-035) |
| Jev 한국어 공시 분류 성능 | 보정과 별개의 측정 대상 |

### 해소된 미결 사항

| 항목 | 결과 |
|---|---|
| Jev 얼리 액세스 확보 여부 | Vercel AI Gateway로 접근 가능. 단 D-026은 다른 근거로 유지 (D-035) |
| Hermes 두뇌로 붙일 모델 선택 | 제공자 무관이라 API 방식. 로컬 GPU 불요 (D-034) |
| 실매매 실행 환경 | AWS 서울, 작은 CPU 인스턴스 (D-036) |

---

## 13. 법적·규정 확인 (미해결)

실전 전환 전에 확인해야 한다. 이 문서 작성자는 투자 자문 자격이 없고, 내용은 시스템 설계 관점의 정리다.

- 사용 증권사 API 약관상 자동매매 허용 범위
- 국내 알고리즘 매매 관련 규정
- 운용 자본 — 없어져도 생활에 지장 없는 금액
