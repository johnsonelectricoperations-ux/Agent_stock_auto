# 에이전트 AI 기반 주식 자동매매 시스템 — 플랜

> 2026-09-24 전면 개정. 에이전트 기반 7프로세스 구조로 재설계.
> 이전 설계(관찰 단계 중심, GitHub Actions 실행)는 폐기했다. 승계·폐기 내역은 context-notes.md D-025 참조.

---

## 1. 목적과 범위

코스피·코스닥 **당일청산 데이트레이딩** 시스템을 만든다.
보유 수십 분~장마감, 오버나이트 없음, 초단위 스캘핑 제외.

**1차 마일스톤은 룰엔진 + 페이퍼 트레이딩이다.** 실전 주문은 그 이후 별도 판단이다.

운용자가 직장 근무 중이라 장중 개입이 불가능하다. 이 제약이 설계 전반을 규정한다.

### 검토 도구

| 도구 | 성격 | 역할 |
|---|---|---|
| Hermes Agent | Nous Research, MIT 오픈소스 | 운영·관제 + 규칙 탐색 연구원 |
| Jev | TypeSafe AI, 확률 판정 모델 | 공시 분류, 진입 전 게이트 |
| 텔레그램 | 메신저 | 리포트·승인, 그리고 킬스위치 |

---

## 2. 핵심 원칙 (절대 규칙)

1. **매매는 자동, 규칙 변경은 사람 승인.**
   규칙은 텔레그램 승인을 거쳐 **새 설정 버전으로만** 반영한다. 실행 중 즉석 변경은 없다.
2. **주문 경로에 LLM을 넣지 않는다.**
   Hermes를 포함한 어떤 LLM도 주문하지 않는다. 결정론적 코드만 주문한다.
3. **킬스위치·강제청산은 LLM이 전부 중단돼도 동작한다.**
   Watchdog은 LLM과 무관한 독립 경로다.
4. **검증되지 않은 파라미터는 `<USER_DEFINED>`로 둔다.**
   임의 기본값 금지. 플레이스홀더가 남아 있으면 기동을 거부한다.

---

## 3. 구성요소와 권한 경계

권한 분리가 이 설계의 핵심이다. **약속이 아니라 구조로 막는다.**

| 구성요소 | 할 수 있는 것 | 할 수 없는 것 |
|---|---|---|
| **Hermes** | 이벤트 로그 DB 읽기, 리포트 생성, 규칙 YAML 제안 | 주문, API 키 접근, DB 쓰기, 최종 홀드아웃 접근 |
| **Jev** | 확률 판정 반환 | 주문, 규칙 변경, 탐색 |
| **Executor** | 주문·체결, **증권사 API 키 단독 보유** | 규칙 판단 |
| **Watchdog** | 강제청산, 킬스위치 | 신규 진입 |
| **Hermes 봇** | 대화·리포트·규칙 승인 요청 | 즉시 주문 |
| **Watchdog 봇** | `/stop` `/flatten` `/status` | 그 외 전부 |

**Hermes가 침해돼도 주문은 불가능해야 한다.** API 키를 Executor만 들고 있는 이유다.

---

## 4. 프로세스 구성

```
[매매 경로 — LLM 없음]
  ① Collector   시세·호가·수급·공시 수집
        ↓
  ② Strategy    룰엔진 → 신호
        ↓
  ③ Jev Gate    확률 판정. 타임아웃 시 폴백      ← 선택적 부품 (D-026)
        ↓
  ④ Risk Mgr    하드 리밋. 통과 못 하면 주문 불가
        ↓
  ⑤ Executor    주문·체결. API 키 단독 보유

[감시 경로]
  ⑥ Watchdog    하트비트, 강제청산, 킬스위치      ← LLM 무관

[운영 경로]
  ⑦ Hermes      읽기 전용

공통: 모든 이벤트 → 이벤트 로그 DB (SQLite, WAL)
```

③은 **없어도 전체가 돌아가야 한다.** Jev 얼리 액세스가 확보되지 않을 수 있기 때문이다 (D-026).

---

## 5. 하루 운영 흐름

| 시점 | 동작 | 실패 시 |
|---|---|---|
| 장전 | 연결·잔고·설정 버전 점검 | **당일 신규 진입 없음** |
| 장중 | 자동 매매 | 아래 장애 대응표 |
| 진입 마감 이후 | 청산만 허용 | — |
| 장마감 | Watchdog이 강제청산 독립 집행 | — |
| 장후 | 증권사 잔고 ↔ 내부 기록 대조 | **불일치 시 다음 날 매매 잠금** |
| 장후 | 복기 리포트 (Hermes) | 리포트 실패는 매매와 무관 |

규칙 승인 요청은 **장후에만** 발송한다. 장중에는 운용자가 볼 수 없다 (D-029).

---

## 6. 장애 대응

| 상황 | 동작 |
|---|---|
| Jev 무응답 | 설정된 폴백 (`skip_entry` / `rules_only`) |
| 시세 끊김 | 신규 진입 중단, **청산 로직은 유지** |
| 증권사 API 끊김 | 즉시 경보. 서버 측 조건주문에 의존 |
| 일 손실 한도 도달 | 전량 청산 + 당일 잠금 |
| 주문 꼬임 | 주문별 고유 ID, 증권사 기준 재동기화 |
| 프로세스 다운 | 하트비트 누락 경보, 재시작 전 잔고 대조 |

**최후의 안전망은 증권사 서버 측 조건주문(스탑로스)이다.**
Watchdog도 프로세스라 죽을 수 있다. 내 서버가 전부 죽어도 증권사 서버는 살아 있다.
→ **서버 측 조건주문 지원 여부가 증권사 선택의 1순위 기준이다** (D-030).

---

## 7. 비용 게이트 — 규칙의 최소 자격

규칙 탐색 루프가 제안하는 모든 규칙은 **백테스트 이전에** 이 게이트를 통과해야 한다.
과거 성적이 좋아도 마찰을 넘지 못하면 구조적으로 불가능하다.

왕복 마찰 `f = 매수수수료 + 매도수수료 + 거래세 0.20% + 슬리피지`
본전 승률 `p = (f/L + 1) / (R + 1)` (L = 손절폭, R = 손익비)

f ≈ 0.33% 기준 요구 승률.

| 손절폭 | 1.5:1 | 2:1 | 3:1 |
|---|---|---|---|
| 0.5% | 66.4% | 55.3% | 41.5% |
| 1.0% | 53.2% | 44.3% | 33.3% |
| 2.0% | 46.6% | 38.8% | 29.1% |

**거래세 0.20%는 고정비다.** 손절폭을 좁힐수록 마찰의 상대 비중이 커져 요구 승률이 급등한다.
촘촘히 끊는 쪽이 안전해 보이지만 구조적으로 전략을 죽인다.

일 왕복 횟수별 월 마찰 소모 (20거래일, 매회 전량 투입, f = 0.33%).
1회 6.6% / 2회 13.2% / 3회 19.8% / 5회 33.0%

구현은 `costgate/`에 있고 테스트로 위 표를 고정해 두었다.

---

## 8. 규칙 자동 탐색 루프

```
Hermes 가설 생성 (YAML 명세)
        ↓
비용 게이트            ← 통과 못 하면 백테스트도 하지 않는다
        ↓
백테스트 (학습 구간)
        ↓
검증 게이트
        ↓
최종 홀드아웃 (1회)    ← Hermes 접근 불가. 파일 권한으로 분리
        ↓
섀도 페이퍼 트레이딩
        ↓
텔레그램 승인          ← 사람
        ↓
실전 (새 설정 버전)
```

### 제약

- 규칙은 코드가 아니라 **사전 정의된 조건 블록 조합의 YAML 명세**로만 제안한다.
- **조건 블록 신규 추가는 승인 사항이다.** Hermes가 임의로 블록을 늘릴 수 없다.
- 실전 규칙 성과가 섀도 대비 기준 이하로 떨어지면 **자동 비활성화**한다.

### 과최적화 방지

- 최종 홀드아웃은 Hermes가 **파일 권한 수준에서** 접근 불가하다. 코드 약속으로는 부족하다.
- **탈락 포함 전체 시험 횟수를 기록**하고 다중검정 보정을 적용한다 (예: Deflated Sharpe).
  분모를 모르면 우연과 실력을 구분할 수 없다.
- 파라미터를 흔들어 견고성을 확인한다. 특정 값에서만 되는 규칙은 기각이다.
- 수수료·거래세·슬리피지를 실제 조건으로 반영한다.

**목표는 "과거 최고 성과"가 아니라 "검증을 통과한 견고한 규칙"이다.**

---

## 9. 실전 전환 단계

| 단계 | 검증 내용 |
|---|---|
| 1. 페이퍼 | 동일 데이터 재실행 결과 일치, 장애 시나리오 강제 재현 |
| 2. 소액 실전 | 페이퍼 대비 슬리피지·체결률 차이 측정 |
| 3. 규모 확대 | 전환 기준 `<USER_DEFINED>` |

---

## 10. 마일스톤

| | 내용 | 완료 기준 |
|---|---|---|
| M0 | 비용 게이트 | 임의 가정에 대해 요구 승률 즉시 산출 **(완료)** |
| M1 | ① Collector + ⑤ Executor(페이퍼) | 하루치 수집·기록이 무인으로 완주. 주문은 전부 가짜 |
| M2 | ② Strategy + ④ Risk Mgr | 신호 생성과 하드 리밋 차단이 합성 시나리오에서 동작 |
| M3 | ⑥ Watchdog + 킬스위치 봇 | **강제 종료 시험에서 포지션이 방치되지 않음** |
| M4 | ⑦ Hermes 리포트 | 읽기 전용임이 파일 권한으로 확인됨 |
| M5 | ③ Jev 게이트 A/B | 유/무 성과가 분리 측정됨 |
| M6 | 규칙 탐색 루프 | 사전 등록 가설 1건의 전 과정 완주 |

**실전 전환은 M6 이후 별도 판단이며 이 문서의 범위가 아니다.**

---

## 11. 범위 밖 (명시적 제외)

- 실거래 주문 — M6 평가 완료 전까지 코드에 존재하지 않는다
- 오버나이트 포지션
- 초~수십초 스캘핑 — 마찰 구조상 배제
- 미국 시장 — 이식하지 않는다. 별도 재설계 사안

---

## 12. 미결 사항

| 항목 | 왜 중요한가 |
|---|---|
| **증권사 API — 서버 측 조건주문 지원 여부** | 무인 운용의 최후 안전망. 증권사 선택 1순위 기준 (D-030) |
| 증권사 API — 웹소켓 지원 | ① Collector 구조를 결정한다 |
| **백테스트용 데이터 범위** | 규칙 탐색 루프 전체의 전제. 기간·분봉 단위·프로그램 매매 수급 포함 여부 |
| Jev 얼리 액세스 확보 여부 | 미확보 시 ③ 없이 진행 (D-026) |
| Jev 한국어 공시 분류 성능 | 성능 미달이면 ③의 가치가 없다 |
| Jev 확률 보정(calibration) 검증 | 보정 안 된 확률은 숫자일 뿐 판정 근거가 못 된다 |
| Hermes 두뇌로 붙일 모델 선택 | 비용과 성능 균형 |

---

## 13. 법적·규정 확인 (미해결)

실전 전환 전에 확인해야 한다. 이 문서 작성자는 투자 자문 자격이 없고, 내용은 시스템 설계 관점의 정리다.

- 사용 증권사 API 약관상 자동매매 허용 범위
- 국내 알고리즘 매매 관련 규정
- 운용 자본 — 없어져도 생활에 지장 없는 금액
