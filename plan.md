# 한국증시 당일청산 데이트레이딩 시스템 — 플랜

> 2026-09-18 개정. 초기안(일봉 스윙 스크리닝)에서 **당일청산 데이트레이딩**으로 전환.
> 전환 근거와 폐기된 결정은 context-notes.md D-011 이후 참조.

## 목적

한국 시장(KOSPI/KOSDAQ)에서 **수십 분~장마감 보유, 당일 전량 청산**하는 매매 시스템을 만든다.
에이전트 AI는 후보 압축·해석·복기를 담당하고, 매매 판단과 리스크 통제는 결정론적 코드가 한다.

**최종 목표는 매일 무인으로 도는 자동 단타매매다.**
운용자가 직장 근무 중이라 장중 개입이 불가능하므로, 사람의 실시간 판단을 전제하지 않는다.
이 제약이 아래 설계 전반을 규정한다.

다만 1차 목표는 수익이 아니라 두 가지다. 여기를 통과하지 못하면 실매매로 가지 않는다.
- 거래 마찰을 넘는 기대값이 존재하는지 확인
- 에이전트 계층이 실제로 기여하는지 분리 측정

## 매매 프로파일 (확정)

| 항목 | 값 |
|---|---|
| 보유시간 | 수십 분 ~ 장마감 |
| 청산 | 당일 전량. 오버나이트 없음 |
| 일 왕복 횟수 | 1~2회 (상한. 근거는 아래 비용 게이트) |
| 손절폭 | 1% 이상 (`risk.stop_loss_pct` 확정 필요) |
| 목표 손익비 | 2:1 이상 |
| 기준봉 | 1분봉 (틱·호가 데이터 불요) |

## 비용 게이트 — 최상위 원칙

**전략 아이디어는 코딩 전에 비용 게이트를 통과해야 한다.**

왕복 마찰 `f = 매수수수료 + 매도수수료 + 거래세 0.20% + 슬리피지`
본전 승률 `p = (f/L + 1) / (R + 1)` (L = 손절폭, R = 손익비)

표준 시나리오 f ≈ 0.33% 기준 월간 마찰 소모 (20거래일, 매회 전량 투입).

| 일 왕복 횟수 | 월 소모 |
|---|---|
| 1회 | 6.6% |
| 2회 | 13.2% |
| 3회 | 19.8% |

요구 승률 (f = 0.33%).

| 손절폭 | 1.5:1 | 2:1 | 3:1 |
|---|---|---|---|
| 0.5% | 66.4% | 55.3% | 41.5% |
| 1.0% | 53.2% | 44.3% | 33.3% |
| 2.0% | 46.6% | 38.8% | 29.1% |

**핵심.** 거래세 0.20%는 고정비다. 손절폭을 좁힐수록 마찰이 상대적으로 커져 요구 승률이 급등한다.
촘촘히 끊는 것이 안전해 보이지만 구조적으로 전략을 죽인다.

## 기본 원칙

1. 에이전트는 룰 엔진에 **입력을 제공**할 뿐, 룰을 우회하거나 한도를 바꿀 수 없다.
2. 에이전트 출력은 JSON 스키마로 강제되고 검증 게이트를 통과해야 한다.
3. 주문 실행, 사이징, 손절, 한도, 장마감 강제청산은 100% 순수 코드다.
4. 검증되지 않은 수치는 임의 기본값 없이 **사용자 정의 필요 플레이스홀더**로 둔다.
5. 모든 성과 평가의 대조군은 **에이전트 없는 동일 룰 엔진**이다.
6. 모든 전략 아이디어는 비용 게이트를 먼저 통과한다.

## 에이전트의 역할 (보유시간이 수십 분이므로 장중 진입 가능)

| 시점 | 역할 | 지연 허용치 |
|---|---|---|
| 장전 | 테마·뉴스·공시 기반 감시 종목 압축 | 분 단위 |
| 장중 | 후보 조건부 재평가, 수급 이상 구간 맥락 부여 | 수 초 (보유시간 대비 무시 가능) |
| 장후 | 매매 복기, 가정이 깨진 지점 식별, 룰 이탈 탐지 | 제한 없음 |
| 주기 | 파라미터 드리프트 감지, 가설 생성 (트랙 2) | 제한 없음 |

**단, 손절·청산·사이징 경로에는 어떤 시점에도 LLM 호출이 들어가지 않는다.**

## 종점 아키텍처 — 모드 분리 (D-024)

관찰·페이퍼·실매매는 **같은 부품을 공유하고 실행기만 교체**한다.
페이퍼에서 검증한 코드와 실매매에서 도는 코드가 동일해야 검증에 의미가 있다.

```
        데이터 레이어 (KIS) ─── 관찰·페이퍼·실매매 공용
                 │
   ┌─────────────┼─────────────┐
 관찰 모드     페이퍼 모드    실매매 모드
 저장만        가짜 체결      진짜 주문
 마감 후 1회   장중 폴링      장중 폴링
   └─────────────┼─────────────┘
                 │
         룰 엔진 (순수 코드) ─── 세 모드 전부 동일
```

실행 환경도 단계별로 다르다 (D-022).

| 단계 | 환경 | 근거 |
|---|---|---|
| 관찰 | GitHub Actions | 장 마감 후 1회면 충분 |
| 실매매 | 상시 실행 환경 | Actions는 정시성·상태유지·상시실행 요건 미충족 |

따라서 모든 로직은 **"언제 실행되는가"와 분리해서** 작성한다.
환경 이전 시 스케줄러만 교체하고 핵심 로직은 그대로 쓴다.

## 무인 운용 안전 원칙 (D-023)

> **내 프로그램이 언제 죽어도 손실이 한정되어야 한다.**

장중 시스템 정지는 미청산 포지션 방치로 이어지고, 이는 당일청산 전제(D-016)의 붕괴다.
운용자가 직장에 있어 인지조차 못 한다. 소프트웨어 내부 로직으로는 해결되지 않는다.

실매매 착수의 **필수 선결 조건**이며, 하나라도 없으면 실매매하지 않는다.

1. 증권사 측 손절 예약 병행 — 내 프로그램이 죽어도 증권사 서버는 살아 있다
2. 하트비트 감시와 운용자 단말 통지
3. 상태 영속화 — 재기동 시 포지션 복구
4. 원격 킬스위치
5. 방치되어도 감당 가능한 자본 상한

## 트랙 분리

시계가 다른 두 작업을 분리한다. 트랙 2는 트랙 1을 블로킹하지 않는다.

```
[트랙 1] 파이프라인 (수 주) ─────────────────▶ 페이퍼 운용
              ▲
              │ 게이트 통과한 가설만 편입
[트랙 2] 가설 발굴 (분기) ── 제안 → 동결 → 전진검증 → 판정 ┘
```

## 한국 시장 고유 제약 (체결 모델 필수 반영)

- 가격제한 ±30%. 상·하한가 도달 시 체결 불가 처리
- 거래정지, 관리종목, 투자경고/위험 상태 변화
- 장 마감 동시호가 처리. 강제청산이 동시호가에 걸리는 경우
- 소형주 호가 단위와 슬리피지
- 일중 시간대별 거동 차이 (시가 변동성, 점심 소강, 마감 전 수급)

## 범위 밖 (명시적 제외)

- 실거래 주문 실행 — 트랙 1 평가 완료 전까지 코드에 존재하지 않는다
- 오버나이트 포지션
- 초~수십초 스캘핑 — 마찰 구조상 배제 (D-011)
- 미국 시장 — 이식하지 않는다. 별도 재설계 사안

## 마일스톤

| | 내용 | 완료 기준 |
|---|---|---|
| M0 | 비용 모델·손익분기 계산기 | 임의 전략 가정에 대해 요구 승률이 즉시 산출됨 **(완료)** |
| O | 관찰 데이터 누적 (D-017) | 무인 수집이 안정 가동되고, 메커니즘 서술이 가능한 후보가 나옴 |
| M1 | 트랙 1 파이프라인 관통 | 임의 과거 구간 재현 가능 실행, A/B 분리 리포트 산출 |
| M2 | 페이퍼 운용 안정화 | 30거래일 무중단, 체결 시나리오 테스트 전량 통과 |
| M3 | 트랙 2 첫 판정 | 사전 등록 가설의 전진검증 완료 및 채택/기각 |
| M4 | 무인 운용 준비 (D-023) | 안전 원칙 5개 전부 구현·검증. 강제 종료 시험에서 포지션이 방치되지 않음 |

**실거래 전환은 M4 이후 별도 판단 사항이다.**
M3까지가 "기대값이 있는가"의 검증이고, M4는 "사람 없이 돌려도 되는가"의 검증이다.
둘은 다른 질문이며 각각 통과해야 한다.
